package v2

import (
	"fmt"
	"strings"
)

// documentParsingGuidance is the shared instruction block injected into any
// phase where the user provides a document file path. All input-driven phases
// share this single definition — add/fix parsing methods here once.
const documentParsingGuidance = `
## 文档解析方法

用户提供了文件路径，请根据文件扩展名选择解析方式：

### .docx 文件
bash(command="python -c \"from docx import Document; doc=Document(r'路径'); print('\\n'.join(p.text for p in doc.paragraphs))\"")
- 如果 python-docx 未安装：bash(command="pip install python-docx && python -c ...")
- 注意：Windows 上用 python，Linux/Mac 上用 python3；如果一个不行换另一个

### .doc 文件（旧格式 Word 97-2003）
Windows（需要已安装 Word 或 WPS）：
bash(command="python -c \"import win32com.client,os; w=win32com.client.Dispatch('Word.Application'); w.Visible=0; d=w.Documents.Open(os.path.abspath(r'路径')); print(d.Content.Text); d.Close(); w.Quit()\"")
- 如果 win32com 未安装：bash(command="pip install pywin32 && python -c ...")
- 备选（需要 LibreOffice）：bash(command="pip install doc2docx && python -c \"from doc2docx import convert; convert(r'路径')\"") 然后用 python-docx 读取生成的 .docx
- 如果都失败：提示用户用 Word 将 .doc 另存为 .docx 格式后重新提供

### .pdf 文件
bash(command="pip install pymupdf && python -c \"import fitz; doc=fitz.open(r'路径'); print('\\n'.join(page.get_text() for page in doc))\"")

### .txt/.md 文件
直接使用 read_file 工具

### 已安装的文档解析 Skill
如果有已安装的文档解析类 Skill（如 doc-parser、any2pdf 等），优先使用 manage_skill(action="run") 调用。

### 重要提示
- Windows 路径在 Python 中用 r'原始字符串' 避免反斜杠转义问题
- 如果 python 命令不存在尝试 python3，反之亦然
- 如果所有方法都失败，告知用户将文件转换为 .docx 或 .txt 格式后重新提供
- 【严禁】说"无法读取文件"或"无法访问本地文件"——你有 bash 工具可以用 Python 解析文档
`

// BuildPhasePrompt constructs the system prompt injection for the current phase.
func BuildPhasePrompt(state *WorkflowState) string {
	if state == nil {
		return ""
	}
	phase := state.ActivePhase()
	if phase == nil {
		return ""
	}

	var sb strings.Builder

	sb.WriteString(fmt.Sprintf("## 当前任务\n\n你正在执行「%s」工作流的「%s」阶段。\n\n", state.Type, phase.Name))
	sb.WriteString(fmt.Sprintf("用户需求：%s\n\n", state.Summary))

	// Resolve effective project path: prefer output_dir from first phase's form data
	// over a useless default like "." which resolves to the app's working directory.
	effectivePath := state.ProjectPath
	if effectivePath == "" || effectivePath == "." {
		if od := resolveOutputDirFromState(state); od != "" {
			effectivePath = od
		}
	}
	if effectivePath != "" && effectivePath != "." {
		sb.WriteString(fmt.Sprintf("项目路径：%s\n\n", effectivePath))
	}

	// Previous phase outputs (truncated)
	prevOutputs := state.PreviousOutputs(500)
	if len(prevOutputs) > 0 {
		sb.WriteString("## 前序阶段产出物（摘要）\n\n")
		for i := 0; i < state.CurrentPhase && i < len(state.Phases); i++ {
			p := state.Phases[i]
			if output, ok := prevOutputs[p.ID]; ok {
				sb.WriteString(fmt.Sprintf("### %s\n%s\n\n", p.Name, output))
			}
		}
	}

	// Inject form data as structured context (when phase has InputSchema + user submitted form)
	if phase.FormData != nil && len(phase.FormData) > 0 {
		sb.WriteString("## ⚠️ 用户提供的结构化信息（必须使用，禁止再询问）\n\n")
		sb.WriteString("以下信息由用户通过表单提交，请**直接基于这些信息**生成本阶段文档。禁止向用户重复索要这些已提供的信息：\n\n")
		// Inject active variant label (tells the LLM which input mode to use).
		if phase.InputSchema != nil {
			if variantID, ok := phase.FormData["_agent_view_variant"]; ok && variantID != nil {
				vid := fmt.Sprintf("%v", variantID)
				for _, v := range phase.InputSchema.Variants {
					if v.ID == vid {
						sb.WriteString(fmt.Sprintf("- **输入方式**：%s\n", v.Label))
						break
					}
				}
			}
		}
		sb.WriteString(RenderFormDataFields(phase, true))
		sb.WriteString("\n")
	}

	// Auto-inject document parsing guidance when form data contains file paths.
	// This is the mechanism-level fix: any phase that receives a file path from
	// the user automatically gets parsing instructions — no need to repeat them
	// in each phaseInstruction case.
	//
	// Two detection paths:
	// 1. Form data contains a file path field/value (user submitted via InputSchema form)
	// 2. No form data but state.Summary contains a file path (user typed path in chat)
	needsParsingGuidance := formDataContainsFilePath(phase.FormData)
	if !needsParsingGuidance && (phase.FormData == nil || len(phase.FormData) == 0) {
		// Check if the user's original request message contains a file path.
		needsParsingGuidance = textContainsFilePath(state.Summary)
	}
	if needsParsingGuidance && !phaseInstructionHasOwnParsingGuidance(phase.ID) {
		sb.WriteString(documentParsingGuidance)
		sb.WriteString("\n")
	}

	// Phase-specific instructions
	sb.WriteString(phaseInstruction(phase.ID))

	return sb.String()
}

// RenderFormDataFields renders the FormData key-value pairs as a bullet list.
// This is the single implementation used by both:
// - BuildPhasePrompt (system prompt injection)
// - GUI's buildFormDataInlinedUserText (user message injection)
//
// boldLabels: when true, renders "- **Label**：value" (system prompt style);
// when false, renders "- Label：value" (user message style).
func RenderFormDataFields(phase *Phase, boldLabels bool) string {
	if phase.FormData == nil || len(phase.FormData) == 0 {
		return ""
	}
	var sb strings.Builder

	renderField := func(label string, value interface{}) {
		s := fmt.Sprintf("%v", value)
		if s == "" || s == "<nil>" {
			return
		}
		if boldLabels {
			sb.WriteString(fmt.Sprintf("- **%s**：", label))
		} else {
			sb.WriteString(fmt.Sprintf("- %s：", label))
		}
		// Multi-line values: indent continuation lines to preserve bullet structure.
		if strings.Contains(s, "\n") {
			lines := strings.Split(s, "\n")
			sb.WriteString(lines[0])
			sb.WriteString("\n")
			for _, line := range lines[1:] {
				if strings.TrimSpace(line) == "" {
					continue
				}
				sb.WriteString("  " + line + "\n")
			}
		} else {
			sb.WriteString(s)
			sb.WriteString("\n")
		}
	}

	if phase.InputSchema != nil {
		allFields := make([]PhaseInputField, 0, len(phase.InputSchema.Fields)+8)
		allFields = append(allFields, phase.InputSchema.Fields...)
		if variantID, ok := phase.FormData["_agent_view_variant"]; ok && variantID != nil {
			vid := fmt.Sprintf("%v", variantID)
			for _, v := range phase.InputSchema.Variants {
				if v.ID == vid {
					allFields = append(allFields, v.Fields...)
					break
				}
			}
		}
		for _, f := range allFields {
			if f.Name == "" || strings.HasPrefix(f.Name, "_") {
				continue
			}
			value, ok := phase.FormData[f.Name]
			if !ok || value == nil || fmt.Sprintf("%v", value) == "" {
				continue
			}
			label := f.Label
			if label == "" {
				label = f.Name
			}
			renderField(label, value)
		}
	} else {
		for key, value := range phase.FormData {
			if key == "" || strings.HasPrefix(key, "_") {
				continue
			}
			renderField(key, value)
		}
	}
	return sb.String()
}

// formDataContainsFilePath returns true if any value in the form data looks like
// a file path (Windows drive letter path, Unix absolute path, or common document
// extensions). This triggers automatic injection of documentParsingGuidance.
func formDataContainsFilePath(formData map[string]interface{}) bool {
	if len(formData) == 0 {
		return false
	}
	for key, val := range formData {
		if key == "" || strings.HasPrefix(key, "_") {
			continue
		}
		s := fmt.Sprintf("%v", val)
		if s == "" || s == "<nil>" {
			continue
		}
		// Signal 1: field name contains "path" or "file" AND value is non-trivial.
		// This is the strongest signal — template authors name file fields explicitly.
		kl := strings.ToLower(key)
		if strings.Contains(kl, "path") || strings.Contains(kl, "file") {
			if len(s) > 2 {
				return true
			}
		}
		// Signal 2: value looks like a Windows drive-letter path (D:\... or C:/...).
		if len(s) >= 4 && ((s[0] >= 'A' && s[0] <= 'Z') || (s[0] >= 'a' && s[0] <= 'z')) &&
			s[1] == ':' && (s[2] == '\\' || s[2] == '/') {
			return true
		}
		// Signal 3: value looks like a Unix absolute path with depth (at least 2 segments).
		// "/home/user/file.pdf" matches; "/yes" does not (too short, no depth).
		if len(s) >= 5 && s[0] == '/' && strings.Count(s, "/") >= 2 {
			// Exclude patterns that are clearly not paths (e.g. URLs handled by web_fetch).
			if !strings.HasPrefix(s, "//") && !strings.Contains(s, "://") {
				return true
			}
		}
		// Signal 4: value ends with a document file extension.
		sl := strings.ToLower(s)
		for _, ext := range []string{".docx", ".doc", ".pdf", ".pptx", ".ppt", ".xlsx", ".xls"} {
			if strings.HasSuffix(sl, ext) {
				return true
			}
		}
	}
	return false
}

// phaseInstructionHasOwnParsingGuidance returns true if the phase-specific
// instruction already includes document parsing methods, in which case the
// shared documentParsingGuidance should be skipped to avoid redundancy.
func phaseInstructionHasOwnParsingGuidance(phaseID string) bool {
	switch phaseID {
	case "pa_disclosure_parsing":
		return true
	}
	return false
}

// textContainsFilePath checks if a plain text string contains a file path
// or document file extension. Used when no form data is available (user typed
// a file path directly in chat).
func textContainsFilePath(text string) bool {
	if len(text) < 4 {
		return false
	}
	// Check for Windows drive-letter paths anywhere in the text.
	for i := 0; i+2 < len(text); i++ {
		c := text[i]
		if ((c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z')) &&
			text[i+1] == ':' && (text[i+2] == '\\' || text[i+2] == '/') {
			// Ensure this is a word boundary (not mid-word like "abc:\dir").
			if i == 0 || text[i-1] == ' ' || text[i-1] == '\n' || text[i-1] == '\t' || text[i-1] >= 0x80 {
				return true
			}
		}
	}
	// Check for document file extensions.
	sl := strings.ToLower(text)
	for _, ext := range []string{".docx", ".doc", ".pdf", ".pptx", ".ppt", ".xlsx", ".xls"} {
		if strings.Contains(sl, ext) {
			return true
		}
	}
	return false
}

func phaseInstruction(phaseID string) string {
	switch phaseID {
	case "requirements":
		return `## 阶段指令

生成需求文档（Markdown 格式），包含：
- 功能需求
- 非功能需求  
- 边界情况
- 验收标准

信息不足的部分标记为「⚠️ 待确认」。直接生成文档，不要先问澄清问题。

## 重要约束（违反将导致错误）
- 只生成一份需求文档，输出完毕后立即停止。
- 【严禁】输出确认提示语、分隔线或任何后续内容。
- 【严禁】在文档后自己说"好的"或模拟用户确认。
- 你只负责输出文档本身，系统会自动提示用户确认。
`
	case "design":
		return `## 阶段指令

基于已确认的需求，生成技术设计文档（Markdown 格式），包含：
- 架构设计
- 技术选型
- 模块划分
- 接口设计
- 数据结构

## 重要约束（违反将导致错误）
- 只生成一份技术设计文档，输出完毕后立即停止。
- 【严禁】输出确认提示语、分隔线或任何后续内容。
- 【严禁】自己模拟用户确认。
- 你只负责输出文档本身，系统会自动提示用户确认。
`
	case "tasks":
		return `## 阶段指令

基于已确认的设计，生成任务拆分文档。使用以下格式（不要使用表格）：

### T1: 任务标题
- **描述**：具体要做什么
- **涉及文件**：file1.cpp, file2.h
- **依赖**：无 / 依赖 T0
- **优先级**：P0/P1/P2
- **工作量**：预估说明

每个任务必须包含描述、涉及文件、依赖、优先级、工作量五个字段。

## 重要约束（违反将导致错误）
- 只生成一份任务拆分文档，输出完毕后立即停止。
- 【严禁】输出确认提示语、分隔线或任何后续内容。
- 【严禁】在同一次回复中开始写代码或执行任何任务。
- 【严禁】自己模拟用户确认。
- 你只负责任务拆分这一步，后续编码由系统自动调度。
`
	case "implementation":
		return "" // TaskRunner/SubAgent handles this

	case "verification":
		return `## 阶段指令

编码执行已完成。现在进行最终验收：

1. 编译整个项目（确认编译通过无错误）
2. 运行程序（确认启动不崩溃）
3. 检查需求文档中的验收标准是否满足
4. 生成验收报告，列出：
   - 编译结果（通过/失败+错误信息）
   - 运行结果（正常启动/崩溃+错误信息）
   - 各验收标准的通过情况
   - 如有问题，给出修复建议

## 重要约束
- 必须实际执行编译和运行命令，不要只描述。
- 如果编译失败，尝试修复后重新编译（最多3次）。
- 生成完验收报告后停止输出。
`

	case "audience_goal":
		return `## 阶段指令

分析 PPT 的目标受众和演讲目标：
- 受众是谁？（职位、知识水平、关注点）
- 演讲目标是什么？（说服、教育、汇报）
- 核心信息是什么？（一句话总结）
- 期望的行动是什么？

输出完毕后等待用户确认。
`
	case "outline":
		return `## 阶段指令

基于受众和目标，设计 PPT 内容大纲：
- 每页的标题和核心要点
- 逻辑流（开场→问题→方案→证据→结论→行动）
- 预估总页数

输出完毕后等待用户确认。
`
	case "slide_scripting":
		return `## 阶段指令

基于大纲，为每一页编写详细脚本：
- 页面标题
- 要展示的文字内容
- 视觉建议（图表/图片/动画）
- 演讲备注

输出完毕后等待用户确认。
`
	case "ppt_generation":
		return "" // Tool execution phase

	case "problem_discovery":
		return `## 阶段指令

分析产品要解决的核心问题：
- 目标用户是谁？
- 他们面临什么痛点？
- 现有解决方案的不足？
- 机会在哪里？

输出完毕后等待用户确认。
`
	case "user_research":
		return `## 阶段指令

基于问题发现，深入用户研究：
- 用户画像（demographics, behaviors, goals）
- 使用场景
- 关键需求优先级
- 竞品分析

输出完毕后等待用户确认。
`
	case "solution_design":
		return `## 阶段指令

基于用户研究，设计产品方案：
- 核心功能列表
- 信息架构
- 用户旅程
- MVP 定义

输出完毕后等待用户确认。
`
	case "prototype":
		return `## 阶段指令

基于方案，描述原型设计：
- 关键页面线框图描述
- 交互流程
- 视觉风格建议

输出完毕后等待用户确认。
`

	// --- Patent Analysis Workflow ---

	case "tech_parsing":
		return `## 阶段指令

解析用户提供的专利文档/技术资料，提炼核心技术方案。

根据用户输入方式处理：
- **用户提供了文件路径**：先用上方"文档解析方法"中的 bash + Python 命令提取文本内容，再基于提取的文本进行分析。如果上方没有"文档解析方法"section，则自行用 bash 调 Python 解析（pymupdf 读 PDF、python-docx 读 docx）。
- **用户粘贴了文本内容**：直接基于文本内容分析。
- **用户只提供了专利号/关键词**：使用 web_search 搜索专利全文，获取后分析。

## 生成文档内容

基于获取的文本内容和用户表单信息，生成技术解析文档：
1. **专利/技术概述**：技术领域、核心发明点、申请日/公开日（如有）
2. **权利要求分析**（如为专利文件）：
   - 独立权利要求的技术特征拆解
   - 从属权利要求的附加技术特征
   - 权利要求的保护范围评估
3. **技术方案详解**：
   - 关键技术特征（结构/步骤/参数/材料）
   - 技术特征之间的逻辑关系
   - 与已知技术的区别
4. **技术效果**：有益效果、性能数据（如有）

## 重要约束（违反将导致错误）
- 如果用户提供了文件路径，必须先解析文件获取内容，不要凭文件名猜测内容。
- 技术分析必须基于实际文档内容，不要臆造。
- 只生成一份技术解析文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "prior_art":
		return `## 阶段指令

基于技术解析结果，进行现有技术检索和分析。

使用 web_search 搜索相关现有技术：
- 搜索关键技术特征相关的专利和论文
- 搜索竞争对手（如用户提供了）的已公开专利
- 搜索技术领域的背景文献

生成文档内容：
1. **检索策略**：使用的关键词组合、数据库范围
2. **相关现有技术列表**：
   - 专利号/论文标题、申请人/作者、日期
   - 技术内容摘要
   - 与目标专利的相关度评估
3. **技术发展脉络**：该技术领域的发展历程
4. **最接近的现有技术**：识别 1-3 篇最接近的对比文件

## 重要约束
- 使用 web_search 实际搜索，不要凭记忆编造专利号或论文。
- 只生成一份现有技术分析文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "infringement":
		return `## 阶段指令

基于技术解析和现有技术分析，进行侵权风险评估。

生成文档内容：
1. **权利要求对比分析**：
   - 逐项对比目标专利权利要求与分析对象的技术特征
   - 标注"相同特征"、"等同特征"、"缺少特征"
2. **侵权判定分析**：
   - 全面覆盖原则分析
   - 等同原则分析
   - 禁止反悔原则考量
3. **风险等级评估**：高/中/低风险，及判定依据
4. **规避设计建议**（如分析目的为规避）：
   - 可替换的技术特征
   - 替换后的技术效果影响

## 重要约束
- 分析必须基于前两个阶段的实际数据。
- 法律分析需谨慎，标注"本分析仅供参考，不构成法律意见"。
- 只生成一份侵权评估文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "strategy":
		return `## 阶段指令

基于前序分析结果，提出知识产权策略建议。

根据用户的分析目的（侵权风险评估/专利布局/无效宣告等），生成针对性策略：
1. **总体策略建议**：基于分析结论的行动建议
2. **具体措施**：
   - 短期措施（立即可执行）
   - 中期措施（3-6 个月）
   - 长期措施（6-12 个月）
3. **专利布局建议**（如适用）：
   - 核心专利方向
   - 外围专利方向
   - 防御性专利建议
4. **风险控制**：潜在风险及应对预案
5. **成本估算**（如适用）：各项措施的预估投入

## 重要约束
- 策略必须基于前序阶段的分析数据。
- 标注"本建议仅供参考，重要决策请咨询专利律师"。
- 只生成一份策略建议文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "report":
		return `## 阶段指令

整合前序所有阶段的分析结果，生成完整的专利分析报告。

报告结构：
1. **封面信息**：报告标题、分析目的、分析对象、完成日期
2. **摘要**：核心结论（1 段话）
3. **技术解析**：核心技术方案概述（来自阶段一）
4. **现有技术分析**：检索结果和技术发展概况（来自阶段二）
5. **侵权/新颖性评估**：对比分析结论（来自阶段三）
6. **策略建议**：行动建议（来自阶段四）
7. **结论与免责声明**

## 重要约束
- 报告内容必须来自前序阶段的实际产出物，不要新增未经分析的结论。
- 末尾必须包含免责声明："本报告由 AI 辅助生成，仅供参考。涉及法律判断的内容请咨询专利代理师或知识产权律师。"
- 只生成一份报告，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`

	// --- Paper Reproduction Workflow ---

	case "paper_analysis":
		return `## 阶段指令

深度解读目标论文。如果用户未提供论文 PDF 或 URL，立即请求用户提供。

论文解读文档应包含：
1. **论文基本信息**：标题、作者、发表会议/期刊、年份
2. **核心方法**：算法/模型架构、关键创新点、与前人工作的区别
3. **实验设置**：数据集（名称、来源、规模）、评价指标、基线方法、超参数设置
4. **关键基准指标（必须精确提取）**：
   - 主指标名称及论文报告的最优值（如 "Accuracy: 85.3%", "BLEU: 32.1", "F1: 91.2"）
   - 各数据集上的结果（如有多个数据集）
   - 各基线方法的对比数值
   - 以表格形式呈现，每行：方法名 | 指标1 | 指标2 | ...
5. **复现关键点**：可能的难点、论文中不够详细需要推断的细节
6. **代码/数据线索**：论文中提到的官方代码仓库、数据集下载链接

## 关键产出：基准指标 JSON

在解读文档末尾，必须输出一个结构化的基准指标块（后续阶段会引用此数据作为达标判定依据）：

[BASELINE_METRICS]
- 主指标名称：（如 Accuracy / F1 / BLEU / mAP）
- 论文最优值：（精确数值，如 85.3）
- 单位/方向：（% 越高越好 / 越低越好）
- 数据集：（在哪个数据集上的结果）
- 次要指标（如有）：指标名=数值, 指标名=数值
[/BASELINE_METRICS]

## 重要约束（违反将导致错误）
- 如果用户已提供论文 URL：
  - arXiv 链接（arxiv.org/abs/XXXX）：将 /abs/ 替换为 /pdf/ 获取 PDF 全文（如 https://arxiv.org/pdf/2401.12345）
  - 其他学术网站：尝试直接 web_fetch，如果只得到摘要页，搜索该论文的 PDF 链接或预印本
  - 使用 web_fetch 获取论文完整内容进行分析
- 如果用户上传了 PDF，直接分析附件内容。
- 实验数据必须精确提取（具体数值，不要概括为"较好"/"有提升"等模糊表述）。
- 基准指标必须是论文中该方法的最优结果，不是基线方法的结果。
- 只生成一份解读文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "reproduction_plan":
		return `## 阶段指令

基于论文解读，制定完整的复现规划。需要实际上网搜索源码和数据集。

复现规划文档应包含：
1. **源码搜索结果**：
   - 在 GitHub/GitLab 搜索官方实现和第三方实现
   - 列出找到的仓库（URL、star 数、最后更新时间、框架）
   - 如果无现成代码，列出需要从头实现的模块
2. **数据集搜索结果**：
   - 论文使用的每个数据集的下载地址
   - 数据集大小、格式、预处理需求
   - 替代数据集（如官方不可用）
3. **硬件需求评估**：GPU 显存、磁盘空间、训练时间预估
4. **依赖环境**：Python 版本、PyTorch/TensorFlow 版本、CUDA 版本、关键库
5. **项目目录结构**：

   paper_reproduction/
     src/          -- 源码
     data/         -- 数据集
     checkpoints/  -- 模型权重
     logs/         -- 训练日志
     results/      -- 实验结果
       tables/     -- 结果表格
       figures/    -- 图表
     reports/      -- 实验报告

6. **复现步骤清单**：按顺序列出从 0 到得到结果的所有步骤
7. **风险与备选方案**：可能失败的环节及应对措施

## 重要约束
- 必须使用 web_search 实际搜索源码和数据集，不要凭记忆编造链接。
- 数据集链接必须经过验证（尝试 web_fetch 确认可访问）。
- 只生成一份规划文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "env_and_data":
		return `## 阶段指令

在远程服务器上搭建实验环境并准备数据。

如果用户尚未提供远程服务器信息（IP/域名、用户名、密码），使用 ask_user 工具请求：
- 服务器 IP 或域名（含端口）
- 用户名
- 密码或密钥
- 工作目录（可选，不提供则自动选择空间最大的分区）

执行步骤：
1. SSH 连接远程服务器
2. 检查 GPU（nvidia-smi）、磁盘空间（df -h）、已有环境
3. 如用户未指定工作目录，找到空间最大的分区并创建项目目录
4. 创建项目目录结构（按复现规划中的结构）
5. 克隆源码仓库（或创建代码骨架）
6. 创建 conda/venv 环境，安装依赖
7. 下载数据集并进行必要的预处理
8. 验证环境：import 关键库成功、GPU 可用、数据集文件完整

## 重要约束
- 所有操作通过 SSH 在远程服务器执行。
- 大文件下载使用后台任务（nohup/screen）。
- 每个步骤完成后验证结果，失败则尝试修复。
- 环境搭建完成后输出一份简要的环境报告（GPU 信息、Python 版本、已安装库、数据集状态）。
`
	case "baseline_reproduction":
		return `## 阶段指令

在远程服务器上复现论文的基线实验。

执行步骤：
1. 按论文的超参数设置配置训练/推理脚本
2. 先跑一个小规模测试（少量 epoch/数据子集）确认代码无 bug
3. 启动完整训练/推理（使用后台任务）
4. 监控训练过程（loss 曲线、GPU 利用率）
5. 训练完成后评估模型，提取指标
6. 与论文报告的数值对比

目标：复现结果与论文报告的数值相差不超过 ±2%（或合理范围内）。

## 重要约束
- 长时间训练使用 SSH 后台任务（submit_task），定期 check_task 监控。
- 记录所有超参数和运行命令到 logs/ 目录。
- 保存模型 checkpoint 到 checkpoints/ 目录。
- 实验结果保存到 results/ 目录（JSON/CSV 格式）。
- 完成后输出基线结果对比表（论文数值 vs 复现数值）。
`
	case "iterative_improvement":
		return `## 阶段指令

在基线复现的基础上，全自动迭代改进实验结果。本阶段设计为无人值守的自动科研模式——设定好目标后持续运行，只在关键节点通知用户。

## 启动前参数确认

在开始改进之前，使用 ask_user 一次性确认以下参数（用户未指定的用默认值）：
- **目标超出值**：主指标超越论文多少时通知用户，默认 0.1%（如论文 85.0% → 达到 85.1% 即通知）。用户可根据领域调整（如 NLP 任务可设 0.5%，CV 任务可设 0.1%）
- **最大运行时间**：总实验运行时间上限，默认 48 小时
- **最大改进轮数**：防止无限循环，默认 50 轮
- **平台期容忍轮数**：连续多少轮无改善后暂停请求用户指导，默认 8 轮

确认后开始全自动运行，不再每轮打断用户。

## 自动改进循环

每轮（完全自主决策，无需用户参与）：
1. 分析历史实验数据（所有已完成轮次的配置→结果映射），识别最有潜力的改进方向
2. 通过 SSH 修改远程服务器上的源码（使用 ssh exec 执行 sed/python 脚本/heredoc 写入等方式编辑文件）
3. 启动训练（SSH 后台任务）
4. 等待训练完成，收集结果
5. 更新结果记录（results/exp_NNN/）
6. 判断是否触发通知条件

源码修改规范：
- 每次修改前先 ssh exec "cat 文件" 确认当前内容
- 小改动用 sed -i 或 python -c 脚本
- 大改动用 heredoc 重写整个文件或关键函数
- 修改后 ssh exec "python -c 'import module'" 验证语法正确
- 记录每轮修改的 diff 到 results/exp_NNN/changes.patch

## 通知用户的条件（仅以下情况才打断用户）

通知必须通过 IM 通道（飞书/微信/QQ）推送，确保用户不在电脑前也能收到。使用 ask_user 工具发送通知——系统会自动将消息推送到用户绑定的 IM 通道（飞书/微信/QQ）。

1. **达成目标**：主指标超越论文 ≥ 目标超出值 → 推送通知"🎉 目标达成！当前最佳 XX.X%（论文 YY.Y%，超出 +Z.Z%）"，询问是继续冲击更高结果还是生成报告
2. **时间到期**：累计运行时间 ≥ 最大运行时间 → 推送通知"⏰ 时间到期（已运行 Nh）。当前最佳 XX.X%，共完成 N 轮"，询问是延长还是停止
3. **平台期**：连续 N 轮主指标无改善（波动 < 0.1%）→ 推送通知"📊 遇到平台期（连续 N 轮无进展）。当前 XX.X%，已尝试：[方向列表]"，请求用户给出新方向
4. **轮数用尽**：完成最大改进轮数 → 推送通知"🔄 已完成全部 N 轮改进。最佳 XX.X%（基线 YY.Y%）"

通知格式要求：
- 第一行：关键数据（指标值、对比），用户扫一眼就知道状态
- 第二行：简要说明
- 最后一行：可选操作（"回复'继续'/'停止'/'换方向:XXX'"）
- 控制在 5 行以内，方便手机阅读

除以上 4 种情况外，全程自动运行不通知用户。

## 用户介入后的处理

用户在收到通知后可以：
- 指定新的改进方向（如"试试 contrastive learning"、"换 AdamW 为 LAMB"）→ 按指定方向继续自动循环
- 调整参数（如"再跑 24 小时"、"目标改为 5%"）→ 更新参数后继续
- 说"继续"→ AI 自主选择方向继续
- 说"停止"/"生成报告"→ 进入实验报告阶段

## 自主改进策略（AI 无人值守时的决策逻辑）

按优先级和实验阶段选择：
- 前 5 轮：超参数网格搜索（学习率、batch size、weight decay）
- 第 6-15 轮：数据增强 + 训练策略（mixup、label smoothing、scheduler）
- 第 16-30 轮：模型架构调整（attention、残差、宽度/深度）
- 第 31+ 轮：组合最优配置 + 消融验证

每轮选择依据：从 results/ 中分析哪个维度改进幅度最大但尚未充分探索。

## 实验记录规范

每个实验目录 results/exp_NNN/：
- config.json：完整超参数和修改说明（"why": 为什么选这个方向）
- metrics.json：所有评估指标
- train.log：训练日志摘要（首尾各 50 行 + 最终指标）
- delta.txt：与基线和历史最佳的对比

## 重要约束
- 全自动模式下严禁每轮都用 ask_user 打断用户。
- 所有训练使用 SSH 后台任务（submit_task），通过 check_task 监控。
- 记录改进决策的理论依据（写入 config.json 的 "why" 字段）。
- 维护一个 results/summary.json 记录所有轮次的关键指标对比表。
- 达到通知条件时用 ask_user 通知，附简洁的数据摘要（3-5行），不要长篇大论。
`
	case "experiment_report":
		return `## 阶段指令

生成完整的实验报告，包含所有实验的原始数据和可视化材料。

报告内容：
1. **实验概述**：论文信息、复现目标、环境配置
2. **基线复现结果**：与论文数值的对比表
3. **改进实验汇总**：
   - 对比实验表（所有变体 vs 基线 vs 论文）
   - 消融实验表（各组件贡献）
   - 超参数影响分析（关键超参数 vs 指标的关系）
4. **图表**：
   - 训练曲线（loss/metric vs epoch）
   - 性能对比柱状图
   - 超参数敏感性曲线
5. **结论**：最佳配置、关键发现、与论文差异分析
6. **产出物清单**：
   - 代码位置（远程服务器路径）
   - 最佳模型 checkpoint 路径
   - 数据集路径
   - 所有实验日志位置

产出物保存到项目的 reports/ 目录。同时在本地（用户工作目录）生成报告文件。

## 重要约束
- 实验数据必须精确，从 results/ 目录读取实际结果。
- 图表数据保存为 CSV（results/tables/），方便后续使用。
- 报告使用 Markdown 格式，图表用描述性文字+数据表格。
- 只生成一份完整报告，输出完毕后等待用户确认。
`
	// ---------------------------------------------------------------------------
	// 长江学者申报书 (Changjiang Scholar Application)
	// ---------------------------------------------------------------------------
	case "cj_personal_profile":
		return `## 阶段指令

梳理申请人的个人资质与申报条件，确认是否满足长江学者各层次（特聘教授/讲座教授/青年学者）的基本要求。

请用户提供以下信息（如未提供，用 ask_user 工具请求）：
- 姓名、性别、出生日期、国籍
- 现工作单位、职称、学科领域
- 学历/学位（本科、硕士、博士、博士后）及毕业院校
- 申报层次（特聘教授/讲座教授/青年学者）

生成文档内容：
1. **基本信息表**：个人信息、联系方式、学历经历
2. **申报条件对照**：逐项对照教育部公布的申报条件，标注满足/不满足/待确认
3. **学科归属分析**：明确一级学科、二级学科，分析学科竞争态势
4. **申报层次建议**：根据年龄、成果等综合分析建议申报的层次
5. **时间线**：关键时间节点（申报截止日期、材料准备周期等）

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语、分隔线或任何后续内容。
- 【严禁】自己模拟用户确认。
`
	case "cj_academic_achievements":
		return `## 阶段指令

系统性整理申请人的学术成就与代表性成果，为申报书的核心部分"学术成就与贡献"提供素材。

生成文档内容：
1. **研究方向凝练**：2-3 个主要研究方向，每个方向用一句话概括核心贡献
2. **代表性论文**（10 篇以内）：
   - 论文标题、期刊/会议、年份、影响因子/CCF 等级
   - 引用次数、学术贡献（1-2 句话）
   - 本人在该成果中的具体贡献
3. **代表性项目**：
   - 项目名称、来源、经费、起止时间、本人角色
4. **学术影响力指标**：
   - H-index、总引用数、ESI 高被引/热点论文
   - 学术兼职（期刊编委、学会理事等）
   - 国际学术交流（受邀报告、合作研究等）
5. **获奖情况**：省部级以上奖励、学术荣誉
6. **知识产权**：授权专利、软件著作权（如有）

## 写作要点
- 成果要突出"原创性"和"国际影响力"
- 每个成果要明确"解决了什么科学问题"
- 数据要准确，引用数建议注明查询日期
- 按研究方向分类组织，体现系统性

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "cj_research_plan":
		return `## 阶段指令

撰写聘期研究计划（通常为 5 年），展示未来的研究蓝图和预期突破。

生成文档内容：
1. **研究背景与意义**：
   - 本领域的国际前沿和关键科学问题
   - 现有研究的不足和突破口
2. **研究目标**：
   - 总体目标（1 段）
   - 具体目标（3-5 个，可量化）
3. **研究内容与技术路线**：
   - 3-4 个相互关联的研究内容
   - 每个内容的关键科学问题、研究方法、预期结果
   - 技术路线图（文字描述研究内容间的逻辑关系）
4. **创新点**：3-4 个明确的创新之处
5. **预期成果**：
   - 论文发表计划（数量、期刊层次）
   - 项目申请计划
   - 人才培养计划
   - 社会服务/成果转化计划
6. **年度安排**：5 年逐年的里程碑计划
7. **研究条件**：现有平台、设备、团队支撑

## 写作要点
- 研究方向要有前瞻性，体现"引领"而非"跟踪"
- 目标要有挑战性但可实现，避免过于保守或好高骛远
- 内容之间要有内在逻辑联系，体现系统性
- 与前序阶段的"学术成就"形成连贯叙事（过去→现在→未来）

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "cj_talent_cultivation":
		return `## 阶段指令

撰写人才培养与团队建设方案，展示申请人在育人方面的理念和成效。

生成文档内容：
1. **已培养人才**：
   - 已毕业硕士/博士人数及去向
   - 学生获奖/成果情况
   - 代表性培养案例
2. **在读学生**：当前指导的研究生情况
3. **聘期培养计划**：
   - 计划招收硕士/博士研究生数量
   - 培养目标和质量标准
   - 培养模式创新（如国际联合培养、产学研合作等）
4. **团队建设方案**：
   - 现有团队结构
   - 拟引进/培养的人才类型
   - 团队梯队建设规划
5. **教学工作**：
   - 承担的课程
   - 教学改革/教材编写（如有）
6. **学科建设贡献**：对所在学科发展的规划和承诺

## 写作要点
- 体现"甘为人梯"的师德师风
- 量化已有培养成效
- 培养计划要与研究方向紧密结合
- 团队建设要有梯度，避免"一人团队"

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "cj_recommendation_summary":
		return `## 阶段指令

整合前四个阶段的内容，生成完整的申报书框架，并撰写推荐意见模板。

生成文档内容：
1. **申报书完整框架**：
   - 按教育部申报书格式要求整合所有内容
   - 标注各部分的字数限制和当前字数
   - 检查前后一致性（数据、时间、项目编号等）
2. **推荐意见模板**（供推荐专家参考）：
   - 学术评价（300-500 字）
   - 推荐理由
   - 建议的措辞和重点
3. **自查清单**：
   - 材料完整性检查
   - 数据准确性检查
   - 格式规范性检查
   - 常见扣分项排查
4. **改进建议**：基于全文审阅的修改建议优先级列表

## 写作要点
- 推荐意见要具体，避免空洞评价
- 自查清单要可操作，每项给出是/否判断
- 整合时注意消除前后矛盾

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	// ---------------------------------------------------------------------------
	// 长江学者评审 (Changjiang Scholar Review)
	// ---------------------------------------------------------------------------
	case "cj_completeness_check":
		return `## 阶段指令

检测申报材料的基本信息完整性。用户需先提供申报书文件（上传 PDF/Word 或粘贴文本内容）。

检测内容：
1. **必填信息检查**：逐项检查是否遗漏
   - 个人基本信息（姓名、性别、出生日期、国籍、学位、职称等）
   - 工作单位信息
   - 学历教育经历
   - 工作经历
   - 学术兼职
2. **格式规范检查**：
   - 是否符合教育部规定的格式要求
   - 字数是否超限
   - 时间格式是否统一
3. **逻辑一致性**：
   - 时间线是否连贯
   - 数据前后是否矛盾
   - 引用编号是否对应
4. **附件材料检查**：应附未附的证明材料清单

输出格式：检查项 + 状态（✅通过/⚠️待补充/❌缺失）+ 具体说明

## 重要约束（违反将导致错误）
- 只生成一份评估文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "cj_achievement_evaluation":
		return `## 阶段指令

评估申报材料中学术成果的质量和竞争力。

评估维度：
1. **论文质量评估**：
   - 代表作的期刊层次（Nature/Science 子刊？领域 Top 期刊？）
   - 引用影响力（H-index、单篇最高引用）
   - 是否有 ESI 高被引/热点论文
   - 通讯/第一作者比例
2. **成果系统性**：
   - 研究方向是否聚焦
   - 成果之间是否有内在逻辑
   - 是否形成了自己的理论体系/方法体系
3. **学术影响力**：
   - 国际学术认可度（受邀报告、编委、评审等）
   - 研究成果的实际应用/转化情况
   - 在本领域的地位和排名预估
4. **与同层次申报者对比**：
   - 本学科近年获批者的成果水平参考
   - 申请人的竞争优势和不足
5. **改进建议**：如何更好地呈现和组织成果

输出：每个维度给出评分（A/B/C/D）+ 详细分析 + 改进建议

## 重要约束（违反将导致错误）
- 只生成一份评估文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "cj_plan_feasibility":
		return `## 阶段指令

评估聘期研究计划的可行性、前瞻性和竞争力。

评估维度：
1. **研究方向前瞻性**：
   - 是否代表国际前沿
   - 是否有明确的科学问题
   - 是否能引领而非跟踪
2. **目标合理性**：
   - 目标是否可量化、可评估
   - 难度是否适中（有挑战但可实现）
   - 与个人积累的匹配度
3. **技术路线可行性**：
   - 方法是否成熟可靠
   - 是否有备选方案
   - 时间安排是否合理
4. **创新点评估**：
   - 是否真正创新（vs 渐进改良）
   - 创新点是否有支撑（前期工作、可行性论证）
5. **与成果的连贯性**：
   - 过去→现在→未来的逻辑链是否通顺
   - 是否体现了"从积累到突破"的发展态势
6. **改进建议**：研究计划的具体修改方向

## 重要约束（违反将导致错误）
- 只生成一份评估文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "cj_narrative_quality":
		return `## 阶段指令

评估申报材料的整体撰写质量和表达效果。

评估维度：
1. **逻辑结构**：
   - 各部分之间的逻辑关系是否清晰
   - 论证是否充分有力
   - 是否有说服力
2. **语言表达**：
   - 是否简洁准确
   - 术语使用是否规范
   - 是否有语法/拼写错误
3. **亮点突出度**：
   - 核心贡献是否一目了然
   - 是否有效使用了数据支撑
   - 关键成就是否被埋没
4. **格式规范**：
   - 字体、字号、间距是否统一
   - 图表是否清晰、有标题和来源
   - 参考文献格式是否规范
5. **阅读体验**：
   - 评审专家 5 分钟能否抓住要点
   - 结构是否便于快速浏览
   - 重点是否有视觉突出

输出：各维度评分 + 具体问题标注 + 修改建议（按优先级排列）

## 重要约束（违反将导致错误）
- 只生成一份评估文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "cj_improvement_report":
		return `## 阶段指令

整合前四个评审阶段的结论，生成综合评估报告和修改建议。

报告内容：
1. **综合评分**：
   - 各维度评分汇总表
   - 综合评级（A/B/C/D）和获批概率预估
   - 与同学科近年获批者的对比定位
2. **优势总结**：申报材料最突出的 3-5 个亮点
3. **主要问题**：按严重程度排列的问题清单
4. **修改建议（优先级排列）**：
   - P0（必须修改）：直接影响获批的硬伤
   - P1（强烈建议）：显著提升竞争力的改进
   - P2（建议改进）：锦上添花的优化
5. **修改示例**：对 P0 问题给出具体的修改前→修改后对比
6. **后续行动清单**：按时间顺序的修改工作计划

## 免责声明
报告末尾必须包含：本评估仅为 AI 辅助参考，不代表正式评审意见。最终评审结果以教育部官方评审为准。

## 重要约束（违反将导致错误）
- 只生成一份综合报告，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	// ---------------------------------------------------------------------------
	// 杰青申请书 (NSFC Distinguished Youth Fund Application)
	// ---------------------------------------------------------------------------
	case "dy_eligibility":
		return `## 阶段指令

评估申请人是否满足国家杰出青年科学基金（杰青）的申报条件。

请用户提供以下信息（如未提供，用 ask_user 工具请求）：
- 姓名、性别、出生日期（杰青要求男性未满 45 周岁，女性未满 48 周岁）
- 国籍、现工作单位、职称
- 学历/学位及毕业院校
- 研究领域/学科代码
- 是否具有高级专业技术职务或博士学位
- 前期是否获得过优青/面上/重点等 NSFC 资助

生成文档内容：
1. **基本信息表**：个人信息完整记录
2. **申报条件逐项对照**：
   - 年龄条件（计算截止日期时的精确年龄）
   - 学历/职称条件
   - 国籍/工作单位条件
   - 回避条件（是否有在研杰青等限制）
3. **学科代码确认**：
   - NSFC 学科分类对应
   - 所申报学部的竞争态势分析
   - 近年该学科资助率参考
4. **申报策略建议**：
   - 建议申报的具体学科代码
   - 需要注意的申报细节
   - 时间节点提醒

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "dy_research_foundation":
		return `## 阶段指令

系统整理申请人的研究工作基础与学术贡献，这是杰青申请书的核心竞争力所在。

生成文档内容：
1. **研究方向概述**：
   - 2-3 个核心研究方向，每个方向的学术定位
   - 各方向之间的内在联系
2. **代表性成果**（5-10 项关键成果）：
   - 每项成果包含：科学问题→方法创新→主要结果→学术影响
   - 强调原创性贡献和"第一/通讯"角色
3. **学术影响力量化**：
   - 论文发表统计（SCI/EI、影响因子分布、引用总数）
   - H-index 及同行对比
   - ESI 高被引论文（如有）
   - 国际学术任职（编委、审稿人、学会职务）
4. **项目经历**：
   - NSFC 项目（面上、重点、优青等）
   - 国家级/省部级项目
   - 经费总额
5. **获奖及荣誉**：
   - 国家级/省部级科技奖励
   - 人才称号（优青、青年拔尖等）
   - 其他重要荣誉
6. **学术贡献凝练**（最重要）：
   - 用 3-5 段话概括"申请人在本领域做出了什么独特的贡献"
   - 与国际同行的差异化定位
   - "解决了什么关键科学问题"的叙事主线

## 写作要点
- 杰青强调"已取得突出研究成果的青年学者"，重点是已有成果而非计划
- 成果叙述要突出"在国际上率先/独创/提出"等体现原创性的表述
- 数据要精确，建议标注引用数据的查询日期
- 成果排列要有层次感：从最突出→次突出

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "dy_research_proposal":
		return `## 阶段指令

撰写未来 5 年的研究方案，包含研究目标、内容、方法和创新点。

生成文档内容：
1. **立项依据**：
   - 研究领域的国际前沿动态
   - 关键科学问题的提出
   - 本项目的研究意义
   - 与申请人前期工作的衔接
2. **研究目标**：
   - 总体目标（解决什么核心科学问题）
   - 具体目标（3-4 个可量化的子目标）
3. **研究内容**：
   - 3-4 个研究内容，每个包含：
     - 科学问题
     - 研究思路
     - 关键技术/方法
     - 预期结果
   - 各研究内容间的逻辑关系
4. **研究方案与可行性**：
   - 技术路线（含方案流程图描述）
   - 关键技术难点及解决思路
   - 可行性论证（前期基础、团队条件、平台支撑）
5. **创新点**：3-4 个明确创新点，区分理论创新和方法创新
6. **年度计划**：5 年逐年工作安排
7. **预期成果**：论文、专利、人才培养、应用前景

## 写作要点
- 杰青研究方案要体现"高起点、高目标"
- 创新点必须有充分的前期基础支撑（不是空想）
- 目标要有雄心但不脱离实际
- 要体现从"优秀"到"卓越"的跨越

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "dy_outcomes_budget":
		return `## 阶段指令

撰写预期成果与经费预算部分。

生成文档内容：
1. **预期研究成果**：
   - 理论成果：预期发表论文数量和层次（Nature/Science 子刊？领域 Top？）
   - 方法/技术成果：预期开发的工具、平台、算法等
   - 应用成果：预期的技术转移转化、社会效益
   - 人才培养：预期培养博士/硕士数量
2. **考核指标**（可量化）：
   - 论文：数量 + 期刊层次
   - 项目：后续计划申报的项目
   - 专利/软著：预期数量
   - 人才培养：博士/硕士毕业人数
3. **经费预算**（杰青资助强度通常为 400 万/5 年）：
   - 设备费（科研设备购置和使用）
   - 材料费（实验材料、测试化验费）
   - 差旅费（学术会议、合作交流）
   - 劳务费（研究生助研津贴）
   - 专家咨询费
   - 其他（出版/文献/知识产权等）
   - 各项预算占比是否合理（参考同类获批项目）
4. **经费使用说明**：
   - 大额设备的必要性论证
   - 经费使用与研究内容的对应关系

## 写作要点
- 预算要合理，避免"设备费占 70%"这类明显不合理的分配
- 预期成果要与研究方案匹配，不能脱节
- 考核指标要有挑战性但可完成

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "dy_final_assembly":
		return `## 阶段指令

整合前四个阶段的内容，生成完整的杰青申请书框架，并进行全文润色。

生成文档内容：
1. **申请书完整框架**（按 NSFC 申请书格式）：
   - 基本信息
   - 立项依据与研究内容
   - 研究基础与工作条件
   - 经费预算
   - 其他需要说明的问题
2. **全文一致性检查**：
   - 数据一致性（引用数、项目编号等前后一致）
   - 逻辑一致性（前期基础→研究方案→预期成果的连贯性）
   - 术语统一性
3. **语言润色**：
   - 摘要精炼（中文 400 字以内、英文 300 词以内）
   - 各部分的衔接过渡
   - 删除冗余表述，突出核心内容
4. **自查清单**：
   - 系统填报注意事项
   - 附件材料清单（代表作 PDF、推荐信等）
   - 常见形式审查不通过的原因排查
5. **评审视角预判**：
   - 模拟评审专家可能的质疑点
   - 针对潜在质疑的应对说明
6. **提交前最终确认项**：
   - 字数是否在限制内
   - 参考文献格式
   - 签名盖章等程序性事项

## 写作要点
- 最终版本要做到"让评审专家 10 分钟内理解你的核心贡献"
- 摘要是第一印象，必须精心打磨
- 自查清单要具体到可以逐项打勾

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	// ---------------------------------------------------------------------------
	// 优青申请书 (NSFC Excellent Young Scientists Fund Application)
	// ---------------------------------------------------------------------------
	case "ey_eligibility":
		return `## 阶段指令

评估申请人是否满足国家优秀青年科学基金（优青）的申报条件。

优青与杰青的关键区别：
- 年龄：男性未满 38 周岁，女性未满 40 周岁（比杰青年轻 7-8 岁）
- 经费：200 万元/3 年（杰青 400 万/5 年）
- 侧重点：发展潜力和创新活力（杰青侧重已有突出成就）
- 定位：处于事业上升期的优秀青年学者

生成文档内容：
1. **基本信息表**：个人信息完整记录
2. **申报条件逐项对照**：
   - 年龄条件（精确计算截止日期时的年龄）
   - 学历/职称条件
   - 博士毕业年限（通常要求博士毕业后有一定独立工作经历）
3. **学科竞争态势**：
   - 所申报学部和学科的近年资助率
   - 同学科近年获批者的典型画像
4. **与杰青的区分定位**：
   - 为什么申请优青而非杰青（年龄/积累阶段的考量）
   - 优青获批后到杰青的发展规划
5. **申报策略建议**

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "ey_research_accumulation":
		return `## 阶段指令

展示申请人的研究积累与发展潜力。优青的核心评价标准是"创新潜力"——不仅看已有成果，更看成长速度和未来突破可能性。

生成文档内容：
1. **研究方向概述**：
   - 1-2 个聚焦的研究方向（优青阶段方向宜精不宜散）
   - 研究方向的前沿性和发展空间
2. **代表性成果**（5-8 项）：
   - 重点突出"独立后"（博士毕业后/建立课题组后）的成果
   - 通讯作者/第一作者论文占比
   - 每项成果的创新贡献（1-2 句话）
3. **成长轨迹**：
   - 博士/博后期间的基础 → 独立后的突破
   - 近 3 年的加速发展证据（论文质量提升、引用增长、大项目获批）
4. **学术影响力**：
   - H 指数及同年龄段对比
   - 受邀报告、编委、学术兼职
5. **项目经历**：
   - 主持项目（特别是独立主持的）
   - 经费总额
6. **发展潜力论证**（最重要）：
   - 从已有成果到未来突破的逻辑链
   - 为什么现在是爆发前夜
   - 与同行比的独特竞争优势

## 写作要点
- 优青评审看"未来 5 年能走多远"，不仅仅是"过去做了多少"
- 强调"成长速度"——博士毕业 N 年内取得了什么
- 突出"独立能力"——哪些是脱离导师后独立完成的
- 方向要聚焦，避免给评审"什么都做但不深入"的印象

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "ey_research_proposal":
		return `## 阶段指令

撰写 3 年研究方案，展示清晰的研究蓝图和可预期的重要突破。

生成文档内容：
1. **立项依据**：
   - 领域前沿和关键科学问题
   - 现有研究的不足（要具体，不要泛泛而谈）
   - 本项目的切入点和独特视角
2. **研究目标**：
   - 总体目标（1 段，有雄心但 3 年可实现）
   - 具体目标（2-3 个可量化子目标）
3. **研究内容**：
   - 2-3 个紧密关联的研究内容
   - 每个内容的关键科学问题 + 方法 + 预期结果
   - 各内容之间的逻辑递进关系
4. **技术路线**：
   - 方案设计思路
   - 关键技术难点及解决方案
   - 可行性论证（前期积累如何支撑）
5. **创新点**（2-3 个）：
   - 区分"理论创新"和"方法创新"
   - 每个创新点要有前期工作支撑
6. **年度计划**：3 年逐年安排

## 写作要点
- 3 年周期决定了方案不能太大——"一个清晰问题 + 系统解决"
- 目标要让评审觉得"3 年后这个人一定能冲杰青"
- 创新点要与前期工作有明确的递进关系

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "ey_outcomes_budget":
		return `## 阶段指令

撰写预期成果与经费预算（优青资助强度 200 万/3 年）。

生成文档内容：
1. **预期成果**：
   - 论文：预期数量和层次（合理即可，不要过于保守也不要夸大）
   - 方法/工具/平台：预期开发的具体产出
   - 人才培养：指导研究生情况
2. **考核指标**（可量化、3 年可完成）
3. **经费预算**（200 万元/3 年）：
   - 设备费
   - 材料费
   - 差旅费/国际合作交流
   - 劳务费（研究生助研）
   - 其他
   - 各项占比是否合理

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "ey_final_assembly":
		return `## 阶段指令

整合全部内容，生成完整的优青申请书框架并润色。

生成文档内容：
1. **申请书完整框架**（按 NSFC 格式）
2. **摘要精炼**（中文 400 字以内、英文 300 词以内）
3. **全文一致性检查**
4. **自查清单**
5. **评审视角预判**：模拟评审可能的质疑并准备应对

## 写作要点
- 优青摘要要让人一眼看出"这个年轻人有冲劲、有潜力"
- 全文基调：充满活力但不浮躁，有积累但还在上升

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	// ---------------------------------------------------------------------------
	// 国自然青年基金 (NSFC Youth Science Fund Application)
	// ---------------------------------------------------------------------------
	case "yf_rationale":
		return `## 阶段指令

撰写青基项目的"立项依据与研究内容"部分。

青基与面上项目的关键区别：
- 经费少（30 万/3 年），研究内容应更聚焦
- 评审更看重"研究思路是否清晰"而非"已有多少成果"
- 鼓励探索性研究——可以有一定风险

生成文档内容：
1. **研究意义**：为什么这个问题值得研究
2. **国内外研究现状**：
   - 领域前沿进展（综述要精炼，不需要面上那么全面）
   - 现有方法的不足
3. **拟解决的科学问题**：1 个核心科学问题（聚焦！）
4. **研究内容**：2-3 个研究内容（不要太多，30 万做不了太大的事）
5. **研究目标**
6. **创新点**（1-2 个即可，要有新意）

## 写作要点
- 30 万的项目不要写成 300 万的野心——小而精
- 体现独立思考能力——"这是我自己想出来的方向"
- 科学问题要新颖但不脱离现实
- 评审会想"这个年轻人有没有做科研的潜力和品味"

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "yf_foundation":
		return `## 阶段指令

撰写"研究基础与可行性"部分。青基申请人通常刚博士毕业不久，前期积累有限是正常的。

生成文档内容：
1. **申请人学术经历**：
   - 教育背景
   - 博士/博后期间的核心训练
2. **与本项目相关的前期工作**：
   - 博士论文中相关的部分
   - 已发表的相关论文（不需要很多，质量优先）
   - 预实验/初步结果（如有）
3. **可行性分析**：
   - 研究方法是否经过验证
   - 申请人是否具备所需的技术能力
   - 依托单位的支撑条件（导师/合作者的平台、设备）
4. **工作条件**：
   - 实验室/计算资源
   - 指导教师或合作者支持（青基申请人可以有学术指导）

## 写作要点
- 前期工作少不是问题——关键是展示"有能力做"
- 可以适当借力导师/团队平台，但要突出独立贡献
- 如果有预实验数据，哪怕是初步的，也要放进来

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "yf_methodology":
		return `## 阶段指令

撰写"研究方案与技术路线"部分。

生成文档内容：
1. **总体技术路线**
2. **各研究内容的具体方案**：
   - 方法设计
   - 关键步骤
   - 预期结果和评价方式
3. **关键技术难点**（1-2 个）及解决思路
4. **年度计划**（3 年）：
   - 第 1 年：方法建立/基础实验
   - 第 2 年：核心研究
   - 第 3 年：完善/验证/论文撰写
5. **预期成果**：
   - 论文（2-4 篇，合理即可）
   - 方法/工具/数据产出
   - 学生培养

## 写作要点
- 方案要具体可操作——让评审觉得"按这个做确实能出结果"
- 30 万经费决定了方案不能太依赖昂贵的实验
- 有备选方案说明思考周全

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "yf_budget":
		return `## 阶段指令

撰写经费预算（青基 30 万/3 年，部分学科直接费用 20-30 万不等）。

生成文档内容：
1. **经费预算表**：
   - 设备费（青基一般不买大设备）
   - 材料费/测试化验费
   - 差旅费（学术会议 1-2 次/年）
   - 劳务费（研究生助研）
   - 出版/文献费
2. **各项说明**：简要说明用途
3. **年度分配**

## 写作要点
- 30 万经费分配要合理——不要某项占 80%
- 青基不建议买大设备——利用依托单位已有条件
- 劳务费合理即可（研究生 2-3 人）

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "yf_final_assembly":
		return `## 阶段指令

整合全部内容，生成完整的青基申请书并润色。

生成文档内容：
1. **申请书完整框架**（按 NSFC 格式）
2. **摘要**（中文 400 字以内、英文 300 词以内）
3. **全文一致性检查**
4. **自查清单**
5. **评审视角预判**

## 写作要点
- 青基摘要要让人看到"清晰的头脑 + 有趣的问题 + 可行的方案"
- 全文基调：朝气蓬勃、思路清晰、脚踏实地
- 篇幅适中——不需要像面上那么长，精炼为主

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	// ---------------------------------------------------------------------------
	// 国自然面上项目 (NSFC General Program Application)
	// ---------------------------------------------------------------------------
	case "gp_rationale":
		return `## 阶段指令

撰写面上项目的"立项依据与研究内容"部分。这是申请书最核心的部分，评审专家首先看这里。

生成文档内容：
1. **研究意义**：
   - 本研究领域的重要性（国家战略/学科发展/应用需求）
   - 为什么现在需要研究这个问题
2. **国内外研究现状**：
   - 国际前沿进展（按研究方向分类综述）
   - 国内研究现状
   - 现有工作的不足和局限（要具体、有文献支撑）
3. **拟解决的关键科学问题**：
   - 明确列出 1-2 个核心科学问题
   - 每个问题为什么重要、为什么难
4. **研究内容**：
   - 3-4 个研究内容，逻辑递进
   - 每个内容包含：目标、方法概要、预期结果
5. **研究目标**：
   - 总体目标（1 段）
   - 具体目标（可量化）
6. **创新点**（2-3 个）

## 写作要点
- 面上项目核心是"一个完整的研究闭环"——问题明确、方案可行、4 年能完成
- 文献综述要最新（近 3 年为主），体现申请人对领域的深入了解
- 科学问题要凝练——评审专家通过"科学问题是否清晰"判断申请人的学术素养
- 创新点要与"现有不足"形成对应关系

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "gp_foundation":
		return `## 阶段指令

撰写"研究基础与工作条件"部分，展示申请人有能力完成本项目。

生成文档内容：
1. **工作基础**：
   - 与本项目直接相关的前期研究（论文、项目、专利）
   - 已有的预实验结果或初步数据
   - 证明"本项目不是从零开始"的关键证据
2. **申请人简介**：
   - 学术经历
   - 代表性论文（5-10 篇，与本项目相关的优先）
   - 主持/参与的科研项目
3. **工作条件**：
   - 实验平台/计算资源
   - 数据资源
   - 合作单位和团队
4. **项目组成员**：
   - 各成员的分工和互补性
   - 研究生培养计划

## 写作要点
- 评审看的是"你凭什么能做成这件事"
- 前期工作和本项目的关联要明确——不要罗列无关成果
- 团队配置要与研究内容匹配

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "gp_methodology":
		return `## 阶段指令

撰写"研究方案与技术路线"部分。

生成文档内容：
1. **总体技术路线**：
   - 整体研究思路（文字描述逻辑关系）
   - 各研究内容之间的衔接
2. **各研究内容的具体方案**：
   - 每个研究内容的详细方法
   - 关键技术/算法/实验设计
   - 预期结果和评价指标
3. **关键技术难点**：
   - 明确列出 2-3 个技术难点
   - 每个难点的解决思路（必须有可行性论证）
4. **可行性分析**：
   - 前期积累支撑
   - 方法的理论依据
   - 备选方案
5. **年度计划与进度安排**（4 年）：
   - 每年的里程碑和可交付成果
6. **预期成果**：
   - 论文发表计划
   - 方法/工具/数据集产出
   - 学生培养

## 写作要点
- 方案要"具体到可操作"——评审要能判断你真的想清楚了怎么做
- 技术路线要有层次：整体→各部分→关键细节
- 备选方案体现严谨性——不是只有一条路

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "gp_budget":
		return `## 阶段指令

撰写经费预算与年度计划（面上项目通常 50-80 万/4 年）。

生成文档内容：
1. **经费预算总表**：
   - 设备费（需详细说明大额设备）
   - 材料费/测试化验费
   - 差旅费/会议费
   - 劳务费
   - 专家咨询费
   - 其他费用
2. **预算说明**：每项经费的用途和必要性
3. **年度预算分配**：4 年逐年分配
4. **与研究内容的对应关系**

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "gp_final_assembly":
		return `## 阶段指令

整合全部内容，生成完整的面上项目申请书并润色。

生成文档内容：
1. **申请书完整框架**（按 NSFC 面上项目格式）
2. **摘要**（中文 400 字以内、英文 300 词以内）
3. **全文一致性检查**：数据、编号、术语前后统一
4. **自查清单**：
   - 字数限制（立项依据通常建议 3000-5000 字）
   - 参考文献格式
   - 附件材料完整性
5. **评审视角预判**

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	// ---------------------------------------------------------------------------
	// 国自然重点项目 (NSFC Key Program Application)
	// ---------------------------------------------------------------------------
	case "kp_strategic_rationale":
		return `## 阶段指令

撰写重点项目的"战略需求与科学问题凝练"部分。重点项目比面上层次更高，强调国家战略需求和重大科学问题。

生成文档内容：
1. **国家战略需求**：
   - 本研究方向对应的国家重大战略（如科技强国、新质生产力等）
   - 行业/领域的重大技术瓶颈
2. **重大科学问题凝练**：
   - 核心科学问题的提出（1-2 个）
   - 为什么是"重大"科学问题（影响面广、难度高、基础性强）
   - 国际上对此问题的认识演变
3. **国内外研究现状**：
   - 国际前沿（重点突出与本项目的差距）
   - 国内现状和不足
   - 本项目的突破口
4. **项目的总体科学目标**
5. **主要研究内容概述**（课题层面，不需要太细）
6. **预期重大突破**

## 写作要点
- 重点项目的格局要大——"这个问题不解决，领域就无法前进"
- 与面上的区别：面上是"一个具体问题"，重点是"一个系统性的重大问题需要多个课题协同攻关"
- 要体现"非一个人能完成，需要团队协作"的必要性

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "kp_team_foundation":
		return `## 阶段指令

撰写"研究团队与工作基础"部分。重点项目通常涉及多个课题组协作。

生成文档内容：
1. **项目负责人**：
   - 学术经历和代表性成果
   - 组织大型项目的经验
   - 在本领域的学术地位
2. **各课题负责人**：
   - 每人的学术专长
   - 与项目负责人的合作历史
   - 承担的具体角色
3. **团队整体优势**：
   - 学科交叉互补性
   - 既往合作成果
   - 平台和资源整合
4. **工作基础**：
   - 与本项目直接相关的前期研究成果
   - 已有的数据、平台、方法积累
   - 预研结果
5. **工作条件**：
   - 依托单位支撑条件
   - 合作单位条件
   - 大型仪器/计算资源

## 写作要点
- 评审判断"这个团队有没有能力做成这件事"
- 合作关系要有证据（联合论文、共同项目）而非只是名单
- 前期积累要直接支撑本项目，不是泛泛罗列成果

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "kp_research_plan":
		return `## 阶段指令

撰写"研究方案与课题设置"部分。重点项目通常设 3-4 个子课题。

生成文档内容：
1. **总体学术思路**：
   - 从科学问题到研究方案的逻辑
   - 各课题之间的内在关系
2. **课题设置**（每个课题包含）：
   - 课题名称
   - 课题负责人
   - 研究目标
   - 研究内容（2-3 个方面）
   - 技术路线
   - 预期成果
   - 与其他课题的衔接
3. **课题间关联图**（文字描述逻辑关系）
4. **关键科学问题的攻关策略**
5. **年度计划**（5 年逐年，按课题分列）
6. **里程碑节点**：每年的关键考核指标

## 写作要点
- 课题划分要有学术逻辑，不是简单分工
- 每个课题能独立成立，但组合起来产生 1+1>2 的效果
- 课题间接口要清晰（数据流、方法共享、阶段成果衔接）

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "kp_budget_management":
		return `## 阶段指令

撰写经费预算与项目管理计划（重点项目通常 250-350 万/5 年）。

生成文档内容：
1. **总经费预算**：
   - 按科目分列
   - 按课题分列
   - 按年度分列
2. **各课题经费分配**：
   - 每个课题的经费总额和主要用途
   - 课题间经费分配的合理性说明
3. **大额支出说明**：设备购置、国际合作等
4. **项目管理计划**：
   - 组织管理架构
   - 学术交流机制（年度工作会、课题组对接会）
   - 数据/成果共享机制
   - 质量控制和进度监控
   - 知识产权管理

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	case "kp_final_assembly":
		return `## 阶段指令

整合全部内容，生成完整的重点项目申请书并润色。

生成文档内容：
1. **申请书完整框架**（按 NSFC 重点项目格式）
2. **摘要**（中文 400 字以内、英文 300 词以内）
3. **全文一致性检查**
4. **各课题一致性**：确保课题设置与前文科学问题对应
5. **自查清单**
6. **评审视角预判**

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】自己模拟用户确认。
`
	// ---------------------------------------------------------------------------
	// 专利申请 (Patent Application)
	// ---------------------------------------------------------------------------
	case "pa_disclosure_parsing":
		return `## 阶段指令

解析专利交底书，提炼核心技术方案。

用户通过表单提交了输入信息，根据输入方式（_agent_view_variant 字段）进行处理：

**方式一：交底书文件（file_mode）**
- 先用 bash 提取文档文本内容：
  - .docx 文件：bash(command="python3 -c \"from docx import Document; doc=Document('路径'); print('\\n'.join(p.text for p in doc.paragraphs))\"")
  - 如果 python-docx 未安装：bash(command="pip install python-docx && python3 -c ...")
  - .txt/.md 文件：直接使用 read_file
  - .pdf 文件：bash(command="python3 -c \"import subprocess; result=subprocess.run(['pdftotext','路径','-'],capture_output=True,text=True); print(result.stdout)\"")，或者 pip install pymupdf 后提取
- 如果以上方法都失败，告知用户改用"手工输入"方式

**方式二：手工输入（manual_mode）**
- 直接使用表单中的 technical_problem（技术问题）、technical_solution（技术方案）、beneficial_effects（有益效果）生成文档
- 如果提供了 figures_paths 和 figures_descriptions，整合为附图清单

生成文档内容：
1. **技术领域**：明确本发明所属的技术领域（基于表单中的 tech_field）
2. **背景技术分析**：
   - 现有技术方案描述
   - 现有技术的缺陷和不足（要具体，这是撰写权利要求"区别特征"的基础）
3. **技术问题凝练**：本发明要解决的技术问题（1-2 个核心问题）
4. **技术方案提炼**：
   - 核心发明点（独立权利要求的基础）
   - 优选/改进方案（从属权利要求的基础）
   - 关键技术特征列表（结构/步骤/参数/材料）
   - 各技术特征之间的逻辑关系
5. **有益效果**：
   - 每个效果对应哪个技术特征
   - 尽可能量化（提高XX%、降低XX%）
6. **附图清单**：
   - 用户提供的附图列表（文件名和路径）
   - 每张图的内容说明（来自 figures_descriptions 或根据文件名推测）
   - 建议补充的附图（如有缺失）
7. **现有技术对比**（如用户提供了 prior_art）：
   - 对比文件分析
   - 与本发明的区别特征
8. **专利类型适配分析**：
   - 根据技术方案特点，确认选择的专利类型（patent_type）是否合适
   - 发明专利 vs 实用新型的适用性建议

## 重要约束（违反将导致错误）
- 技术方案必须从用户提供的材料中提炼，不要臆造技术内容。
- 区别特征必须明确——这直接影响权利要求的撰写质量。
- 只生成一份解析文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "pa_prior_art_search":
		return `## 阶段指令

基于交底书解析结果，进行现有技术检索（查新），分析本发明的新颖性和创造性。

## 第零步：读取技术方案（必须先执行）

用 list_directory 查看项目目录，然后用 read_file 读取上一阶段生成的交底书解析文档，获取核心技术特征和发明点。

## 检索策略

1. **确定检索关键词**：从技术方案中提取 3-5 组关键词组合（技术领域 + 技术特征 + 技术效果），包括中英文关键词
2. **多源检索**：使用 web_search 在以下来源检索：
   - Google Patents（搜索 "site:patents.google.com" + 技术关键词）
   - WIPO PatentScope（搜索 "site:patentscope.wipo.int" + 关键词）
   - Google Scholar（学术论文中的相关技术方案）
   - 直接搜索中文关键词 + "专利" / "发明"（Google 会索引 CNIPA 公开专利）
3. **对比文件筛选**：选出 3-5 篇最接近的对比文件（专利或论文）
4. **详情获取**：对高相关度结果，使用 web_fetch 获取摘要和权利要求详情

## 文档结构

### 一、检索范围与关键词
- 列出检索使用的关键词组合
- 说明检索的数据库/来源

### 二、相关对比文件列表
| 序号 | 文件编号/名称 | 来源 | 公开日期 | 相关度 | 核心内容摘要 |
|------|-------------|------|---------|--------|-------------|
| D1   | ...         | ...  | ...     | 高/中  | ...         |

### 三、新颖性分析
对每篇高相关度对比文件：
- 本发明的技术特征 vs 对比文件的技术特征
- 区别技术特征（本发明有而对比文件没有的）
- 结论：是否具有新颖性

### 四、创造性分析
- 最接近的现有技术（选一篇作为基础）
- 区别技术特征
- 该区别特征是否是"本领域技术人员容易想到的"
- 技术效果是否有预料不到的改进
- 结论：是否具有创造性

### 五、权利要求撰写策略建议
基于查新结果，建议：
- 独立权利要求应包含哪些技术特征（保证新颖性+创造性的最小特征集）
- 建议避开的技术特征（已被现有技术公开的）
- 建议强调的区别特征

## 重要约束
- 必须实际使用 web_search 进行检索，不得凭记忆编造对比文件。
- 如果检索不到强相关结果，如实报告"未检索到高度相关的对比文件"。
- 只生成一份查新报告，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "pa_claims_drafting":
		return `## 阶段指令

基于技术方案提炼结果和查新检索报告，撰写权利要求书。

## 第零步：读取完整的技术方案和查新报告（必须先执行）

上方"前序阶段产出物（摘要）"中的内容已被截断。撰写权利要求前，必须先用 list_directory 查看项目目录，然后用 read_file 读取：
1. 完整的"交底书解析与技术提炼文档.md"——获取所有技术特征和发明点细节
2. 完整的"查新检索与新颖性分析.md"——获取对比文件、新颖性/创造性分析、权利要求撰写策略建议

撰写权利要求时必须参考查新报告中的策略建议，确保独立权利要求包含足以区别于现有技术的特征集。

## 文档结构

文档分为两部分：

### 第一部分：权利要求书正文（可直接用于申请提交）

按编号直接撰写权利要求，格式示例：
1. 一种XX装置，包括...，其特征在于：...。
2. 根据权利要求1所述的XX装置，其特征在于：...。
3. ...

要求：
- 产品独立权利要求（结构/组成/连接关系）
- 方法独立权利要求（步骤/条件/参数）——如适用
- 采用"两段式"撰写（前序部分 + 特征部分）
- 保护范围要适当上位概括（不要过窄也不要过宽）
- 每个独立权利要求下 5-10 条从属权利要求
- 从宽到窄的层次布局，覆盖优选方案、具体参数、具体结构

### 第二部分：撰写说明（供用户参考，不提交）

- **保护范围层次说明**：解释独立权利要求到从属权利要求的递进关系
- **新颖性/创造性自评估**：与最接近现有技术的区别分析
- **修改建议**：如有需要调整的地方

## 撰写规范
- 每条权利要求为一个完整句子，以句号结尾
- 独立权利要求使用"其特征在于"连接前序和特征部分
- 从属权利要求使用"根据权利要求X所述的..."开头
- 从属权利要求可以引用一条权利要求（单一从属）或多条权利要求（多重从属，如"根据权利要求1-3中任一项所述的..."）
- 多重从属权利要求不得作为另一项多重从属权利要求的引用基础（专利法实施细则第23条）
- 技术术语前后一致
- 避免功能性限定（除非必要）
- 数值范围使用"为...至..."或"为..."
- 方法权利要求中步骤用"步骤一"、"步骤二"或"S1"、"S2"编号

## 重要约束（违反将导致错误）
- 第一部分必须是可直接提交的格式——纯编号权利要求，不含解释性文字。
- 权利要求必须有层次感——独立权利要求最宽，从属权利要求逐步限缩。
- 独立权利要求的前序部分必须包含与最接近现有技术共有的技术特征。
- 文档生成后，必须保存为 Word 文件（见下方"保存为 Word 文件"section）。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。

## 保存为 Word 文件（必须执行）

文档内容输出完毕后，必须立即将权利要求书保存为 .docx 文件。

**严禁将文档内容嵌入 bash command 参数中**——权利要求文本超过 4000 字符会触发 inline payload limit。

正确做法（三步）：

步骤 1：用 write_file 保存权利要求书纯文本为 .md 文件。
write_file(path="OUTPUT_DIR/权利要求书.md", content="1. 一种...", mode="write")
如果内容超过约 6000 字符，建议分多次 mode="append" 写入以避免模型输出截断。
格式：每条权利要求占一段，段落之间用空行分隔。

步骤 2：用 write_file 保存转换脚本（以下内容直接复制，只需替换 OUTPUT_DIR）：
write_file(path="OUTPUT_DIR/md2docx.py", content="import os\nimport re\nfrom docx import Document\nfrom docx.shared import Pt, Cm\nfrom docx.enum.text import WD_ALIGN_PARAGRAPH\n\nd = r'OUTPUT_DIR'\nsrc = os.path.join(d, '权利要求书.md')\ndst = os.path.join(d, '权利要求书.docx')\n\ndoc = Document()\ndoc.styles['Normal'].font.size = Pt(12)\nh = doc.add_heading('权利要求书', level=1)\nh.alignment = WD_ALIGN_PARAGRAPH.CENTER\n\ntext = open(src, encoding='utf-8').read()\nclaims = re.split(r'\\n(?=\\d+\\.\\s)', text.strip())\nfor claim in claims:\n    claim = claim.strip()\n    if not claim:\n        continue\n    p = doc.add_paragraph(claim)\n    p.paragraph_format.first_line_indent = Cm(0.74)\n\ndoc.save(dst)\nprint('saved:', dst)\n", mode="write")

步骤 3：用 bash 安装依赖并执行转换：
bash(command="pip install python-docx -q && python OUTPUT_DIR/md2docx.py")

如果 Python 不可用（pip/python 报错 "not found"），改用 PowerShell COM 方案：
步骤 2（替代）：用 write_file 保存 PowerShell 转换脚本：
write_file(path="OUTPUT_DIR/md2docx.ps1", content="$src = 'OUTPUT_DIR/权利要求书.md'\n$dst = 'OUTPUT_DIR/权利要求书.docx'\n$word = New-Object -ComObject Word.Application\n$word.Visible = $false\ntry {\n    $doc = $word.Documents.Add()\n    $text = Get-Content $src -Encoding UTF8 -Raw\n    $claims = $text -split '(?m)(?=^\\d+\\.\\s)'\n    foreach ($claim in $claims) {\n        $claim = $claim.Trim()\n        if ($claim) { $doc.Paragraphs.Add().Range.Text = $claim }\n    }\n    $doc.SaveAs2([System.IO.Path]::GetFullPath($dst))\n    Write-Output \"saved: $dst\"\n} finally {\n    $doc.Close()\n    $word.Quit()\n    [System.Runtime.Interopservices.Marshal]::ReleaseComObject($word) | Out-Null\n}\n", mode="write")
步骤 3（替代）：执行 PowerShell 脚本：
bash(command="powershell -ExecutionPolicy Bypass -File OUTPUT_DIR/md2docx.ps1")

要求：
- 所有 OUTPUT_DIR 替换为项目路径（在本消息顶部的"项目路径"字段中可以看到）
- 完成后告知用户文件路径
`
	case "pa_description_writing":
		return `## 阶段指令

基于交底书解析、权利要求书和已确认的附图编号，撰写完整的专利说明书。

## 第零步：读取前序阶段产出物（必须先执行）

上方"前序阶段产出物（摘要）"中的内容已被截断，不包含完整的权利要求和技术方案细节。
撰写说明书前，必须先用 read_file 读取项目目录中的以下文件（如存在）：
- 交底书解析与技术提炼文档.md — 完整的技术方案
- 权利要求书.md — 完整的权利要求条文（说明书必须逐条支持）

然后再基于完整内容撰写说明书。

注意：附图编号和标记表已在上一阶段（附图整理）中确定并经用户确认，请直接引用。

文档应为可直接提交的说明书正文格式（按专利说明书标准格式）：

# [发明名称]

## 技术领域

[一段话说明所属领域]

## 背景技术

[现有技术描述 + 缺陷分析。注意：不要贬低现有技术，客观描述不足]

## 发明内容

### 要解决的技术问题

[与独立权利要求对应的技术问题]

### 技术方案

[用说明性语言描述技术方案，与独立权利要求对应但更详细]

### 有益效果

[每个效果对应具体技术特征]

## 附图说明

[逐图描述：图1是...的结构示意图；图2是...的流程图]

## 具体实施方式

[至少 2-3 个实施例：
- 实施例 1：与独立权利要求对应的最基本实施方式
- 实施例 2-3：与从属权利要求对应的优选/变体实施方式
- 每个实施例结合附图标记详细描述
- 工艺参数、材料、尺寸等具体数据]

## 撰写规范
- 说明书必须充分公开技术方案——"所属技术领域的技术人员能够实现"
- 说明书必须支持权利要求——权利要求中的每个特征都能在说明书中找到依据
- 附图标记在全文中统一编号（引用上一阶段确定的标记表）
- 同一技术特征在全文中使用相同术语
- 具体实施方式要有足够细节，包括参数、条件、步骤顺序

## 重要约束（违反将导致错误）
- 输出必须是可直接提交的说明书格式——用户可以直接复制到申请文件中。
- 说明书内容必须与权利要求书对应——不能出现权利要求有而说明书没有的特征。
- 实施例必须具体——不能只是权利要求的重复改写。
- 文档生成后，必须保存为 Word 文件（见下方"保存为 Word 文件"section）。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。

## 保存为 Word 文件（必须执行）

说明书内容输出完毕后，必须立即保存为 .docx 文件。

**严禁将文档内容嵌入 bash command 参数中**——说明书文本超过 4000 字符会触发 inline payload limit。

正确做法（三步）：

步骤 1：用 write_file 保存说明书纯文本为 .md 文件。
write_file(path="OUTPUT_DIR/说明书.md", content="# 发明名称\n\n## 技术领域\n...", mode="write")
如果内容超过约 6000 字符，建议分多次 mode="append" 写入以避免模型输出截断。
格式：使用标准 Markdown（# 标题、正文段落、空行分隔）。

步骤 2：用 write_file 保存转换脚本（以下内容直接复制，只需替换 OUTPUT_DIR）：
write_file(path="OUTPUT_DIR/md2docx_desc.py", content="import os\nfrom docx import Document\nfrom docx.shared import Pt\n\nd = r'OUTPUT_DIR'\nsrc = os.path.join(d, '说明书.md')\ndst = os.path.join(d, '说明书.docx')\n\ndoc = Document()\ndoc.styles['Normal'].font.size = Pt(12)\n\ntext = open(src, encoding='utf-8').read()\nfor block in text.split('\\n\\n'):\n    block = block.strip()\n    if not block:\n        continue\n    if block.startswith('#'):\n        level = min(len(block) - len(block.lstrip('#')), 4)\n        doc.add_heading(block.lstrip('#').strip(), level=level)\n    else:\n        doc.add_paragraph(block)\n\ndoc.save(dst)\nprint('saved:', dst)\n", mode="write")

步骤 3：用 bash 安装依赖并执行转换：
bash(command="pip install python-docx -q && python OUTPUT_DIR/md2docx_desc.py")

如果 Python 不可用（pip/python 报错 "not found"），改用 PowerShell COM 方案：
步骤 2（替代）：用 write_file 保存 PowerShell 转换脚本：
write_file(path="OUTPUT_DIR/md2docx_desc.ps1", content="$src = 'OUTPUT_DIR/说明书.md'\n$dst = 'OUTPUT_DIR/说明书.docx'\n$word = New-Object -ComObject Word.Application\n$word.Visible = $false\ntry {\n    $doc = $word.Documents.Add()\n    $text = Get-Content $src -Encoding UTF8 -Raw\n    foreach ($block in ($text -split '\\n\\n')) {\n        $block = $block.Trim()\n        if (-not $block) { continue }\n        if ($block.StartsWith('#')) {\n            $level = ($block -replace '[^#]','').Length\n            $title = $block.TrimStart('#').Trim()\n            $doc.Paragraphs.Add().Range.Text = $title\n        } else {\n            $doc.Paragraphs.Add().Range.Text = $block\n        }\n    }\n    $doc.SaveAs2([System.IO.Path]::GetFullPath($dst))\n    Write-Output \"saved: $dst\"\n} finally {\n    $doc.Close()\n    $word.Quit()\n    [System.Runtime.Interopservices.Marshal]::ReleaseComObject($word) | Out-Null\n}\n", mode="write")
步骤 3（替代）：执行 PowerShell 脚本：
bash(command="powershell -ExecutionPolicy Bypass -File OUTPUT_DIR/md2docx_desc.ps1")

要求：
- 所有 OUTPUT_DIR 替换为项目路径（在本消息顶部的"项目路径"字段中可以看到）
- 完成后告知用户文件路径
`
	case "pa_figures_organization":
		return `## 阶段指令

基于技术方案和权利要求，生成专利附图并撰写附图说明。

## 第零步：读取技术方案和权利要求（必须先执行）

用 list_directory 查看项目目录，然后用 read_file 读取：
1. 交底书解析文档——获取技术方案描述和组件关系
2. 权利要求书——获取需要在附图中体现的技术特征

同时确认用户是否已提供附图文件（检查 .png/.jpg/.svg/.drawio 等）。

## 附图生成策略

### 情况一：用户已提供附图
- 确认文件存在（list_directory），记录文件名
- 直接进入"附图编号与说明"步骤

### 情况二：用户未提供附图（更常见）
必须使用工具自动生成附图。按以下优先级选择生成方式：

**方式 A：Python + matplotlib/PIL（推荐，无外部依赖）**
使用 bash 执行 Python 脚本生成结构图/流程图：
- 系统架构/模块关系图：用 matplotlib 的 patches + arrows 绘制方框图
- 流程图：用 matplotlib 绘制步骤框和箭头
- 数据流图：用 matplotlib 绘制节点和连线
- 保存为 PNG 格式到项目附图目录

**方式 B：SVG 文件（matplotlib 不可用时的备选）**
用 write_file 将 SVG 源码写入项目目录下的 .svg 文件（如 图1-系统架构.svg）。

**方式 C：Mermaid → PNG（如系统有 mmdc 命令）**
用 write_file 写 .mmd 文件，bash 调用 mmdc 转 PNG。

**方式 D：drawio-skill（如已安装）**
调用 manage_skill 运行 drawio-skill 生成图表。

### 必须生成的附图类型（根据技术方案选择适用的）
1. **系统整体架构图**（几乎所有发明专利都需要）——展示主要模块和连接关系
2. **方法流程图**（如有方法权利要求）——展示步骤顺序和判断逻辑
3. **关键模块详细结构图**（如有复杂子系统）
4. **数据处理/信号流向图**（如涉及数据处理）

### 生成附图的规范
- 图中**不要包含中文文字**——用数字标记（1、2、3...）代替，标记含义在附图说明中解释
- **必须使用黑白线条图**——专利法规定附图"应当使用黑色墨水绘制，不得着色"（《专利审查指南》第一部分第一章4.3节）
- 禁止使用彩色、灰度填充、渐变色——仅使用黑色线条和白色背景
- 剖面可用斜线填充，不同部件可用不同方向/密度的斜线区分
- 线条清晰均匀，粗细一致
- 每张图保存为独立的 PNG 文件，命名为 图1-xxx.png、图2-xxx.png
- 分辨率建议 300 DPI 或 2000x1500 像素以上

## 附图编号与说明文档

生成所有附图后，输出文档内容：

### 一、附图清单
| 图号 | 文件名 | 描述 |
|------|--------|------|
| 图1  | 图1-系统架构.png | 本发明系统整体架构示意图 |
| 图2  | 图2-方法流程.png | 本发明方法实施步骤流程图 |

### 二、附图标记对照表
| 标记号 | 组件名称 | 所在附图 |
|--------|---------|---------|
| 1 | 控制器 | 图1 |
| 2 | 传感器 | 图1 |

### 三、附图说明段落（可直接用于说明书）
图1是本发明实施例提供的XXX系统的整体架构示意图。
图2是本发明实施例提供的XXX方法的步骤流程图。

## 重要约束
- 附图中的标记必须与说明书和权利要求中的组件一一对应。
- 所有生成的图形内容（SVG/PNG/Mermaid）必须用 write_file 或 bash 保存到项目目录下的文件中，然后在文档中引用文件路径。不要在回复中内联图形源码。
- 如果所有绘图方式都失败，明确告知用户需要人工补充正式附图。
- 只生成一份附图整理文档，输出完毕后立即停止。
- 【严禁】输出确认提示语或后续内容。
- 【严禁】自己模拟用户确认。
`
	case "pa_document_assembly":
		return `## 阶段指令

整合所有阶段产出物，组装完整的专利申请文件并进行最终检查。

## 第零步：读取前序阶段产出物（必须先执行）

上方的"前序阶段产出物（摘要）"已被截断。做一致性检查前，先用 list_directory 查看项目目录，然后用 read_file 读取已生成的完整文件（特别是权利要求书.md/docx 和说明书.md/docx）。

## 第一步：生成检查报告

生成文档内容：
1. **完整申请文件清单**：
   - ☐ 请求书（需在专利局系统中填写，此处列出所需信息）
   - ☐ 权利要求书
   - ☐ 说明书
   - ☐ 说明书附图
   - ☐ 说明书摘要
   - ☐ 摘要附图（从说明书附图中选取最能说明技术方案的一幅）
2. **说明书摘要**（300 字以内）：
   - 写明发明名称、技术领域、技术问题、主要技术特征和用途
   - 不得使用商业性宣传用语
3. **请求书信息汇总**：
   - 发明名称
   - 申请人信息（名称、地址、国籍）
   - 发明人信息
   - 联系人信息
   - 专利类型
   - 优先权信息（如有）
4. **一致性全面检查**：
   - 发明名称在各文件中是否统一
   - 权利要求中的技术特征是否全部在说明书中有描述
   - 附图标记是否前后一致
   - 技术术语是否全文统一
   - 从属权利要求引用关系是否正确
   - 说明书各部分是否完整
5. **形式审查预检**：
   - 权利要求是否超过 10 条（超过需额外缴费）
   - 说明书页数
   - 附图数量
   - 是否有明显形式缺陷
6. **审查风险评估**：
   - 发明专利：新颖性风险点（与已知现有技术的对比）、创造性风险点（技术效果是否明显）、建议的应对策略
   - 实用新型：初步审查要点（是否属于产品形状/构造的技术方案、是否实用、权利要求是否清楚）
   - 注意：实用新型不经过实质审查，但授权后可能面临无效宣告，仍需关注新颖性
7. **提交建议**：
   - 提交方式（推荐电子申请，通过 https://cponline.cnipa.gov.cn）
   - 需缴费项目列表（申请费、实审费、权利要求附加费等，具体金额请查询国知局最新收费标准）
   - 时间节点提醒（提交后 18 个月公开，主动请求提前公开可加速）

## 第二步：保存为 Word 文件（必须执行）

前序阶段应该已经分别保存了 权利要求书.docx 和 说明书.docx。请先检查输出目录中是否存在这些文件。

如果文件已存在：
- 使用 bash + python 读取各文件验证内容完整性
- 生成一份"说明书摘要.docx"（300字以内）保存到同目录

如果文件不存在（前序阶段未保存）：
- 从前序阶段的产出物摘要中获取内容
- 分别生成 权利要求书.docx、说明书.docx、说明书摘要.docx 保存到输出目录

检查已有文件：

bash(command="pip install python-docx -q && python -c \"import os; d=r'OUTPUT_DIR'; print('权利要求书:', '存在' if os.path.exists(os.path.join(d,'权利要求书.docx')) else '不存在'); print('说明书:', '存在' if os.path.exists(os.path.join(d,'说明书.docx')) else '不存在')\"")

然后生成说明书摘要（摘要内容较短，可直接内联）：

bash(command="python -c \"from docx import Document; from docx.shared import Pt; import os; doc=Document(); doc.styles['Normal'].font.size=Pt(12); doc.add_heading('说明书摘要',level=1); doc.add_paragraph('实际摘要内容300字以内'); d=r'OUTPUT_DIR'; os.makedirs(d,exist_ok=True); doc.save(os.path.join(d,'说明书摘要.docx')); print('已保存')\"")

注意：如果需要生成较长的文件（如补写权利要求书或说明书），**严禁将 Python 脚本内联到 bash command 参数中**。正确做法：先用 write_file 保存 Python 脚本为 .py 文件，再用 bash 执行该文件。

最终输出目录应包含：
- 权利要求书.docx
- 说明书.docx
- 说明书摘要.docx
- 申请文件组装与检查报告.md（本阶段的检查报告）

## 免责声明
文档末尾必须包含：本文件由 AI 辅助生成，建议提交前由专业专利代理人审核。专利权的最终授予以国家知识产权局审查决定为准。

## 重要约束（违反将导致错误）
- 一致性检查必须逐项核对，不能跳过。
- 摘要必须控制在 300 字以内。
- 必须通过工具调用来生成 .docx 文件——不要把 Python 代码作为文本输出到对话中让用户自己运行。
- 长脚本（>200 字符）先用 write_file 保存为 .py 文件再用 bash 执行；短脚本（如摘要生成，<200 字符）可直接内联 bash。
- OUTPUT_DIR 替换为项目路径（在本消息顶部的"项目路径"字段中可以看到）。
- 完成检查报告 + 保存摘要 docx 后立即停止。
- 【严禁】自己模拟用户确认。
`
	default:
		// Generic instruction for doc-only phases without specific prompts.
		return `## 阶段指令

请基于前序阶段的产出物和用户需求，生成本阶段的完整文档内容（Markdown 格式）。
内容要详实、结构清晰、有可操作性。

## 重要约束（违反将导致错误）
- 只生成一份文档，输出完毕后立即停止。
- 【严禁】输出确认提示语、分隔线或任何后续内容。
- 【严禁】自己模拟用户确认。
- 你只负责输出文档本身，系统会自动提示用户确认。
`
	}
}
