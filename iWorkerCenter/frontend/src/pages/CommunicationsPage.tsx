import { useEffect, useMemo, useState } from 'react';
import { SectionCard } from '../components/cards/SectionCard';
import { DataTable } from '../components/table/DataTable';
import { useI18n } from '../i18n';

type CollabTask = { id: string; title: string; description?: string; from_colleague_id?: string; to_colleague_id?: string; to_role_code?: string; status: string; priority?: number; result?: string; created_at?: string; updated_at?: string };
type Colleague = { id: string; name: string; role_code?: string; role_name?: string; status?: string };
type CollabRow = { id: string; title: string; from: string; to: string; status: string; priority: string; updated: string };
type Message = { kind: 'ok' | 'warn' | 'danger'; text: string } | null;
const emptyForm = { title: '', description: '', from_colleague_id: '', to_colleague_id: '', to_role_code: '', priority: '5' };
async function requestJSON<T>(url: string, init?: RequestInit): Promise<T> { const resp = await fetch(url, init); const text = await resp.text(); const data = text ? JSON.parse(text) : null; if (!resp.ok) throw new Error(data?.error?.message || data?.message || 'Request failed: ' + resp.status); return data as T; }
const shortID = (value?: string) => value ? value.slice(0, 12) : '-';
const workerName = (workers: Colleague[], id?: string) => workers.find(w => w.id === id)?.name || shortID(id);

export function CommunicationsPage() {
  const { t } = useI18n();
  const [tasks, setTasks] = useState<CollabTask[]>([]);
  const [workers, setWorkers] = useState<Colleague[]>([]);
  const [form, setForm] = useState(emptyForm);
  const [loading, setLoading] = useState(false);
  const [busy, setBusy] = useState('');
  const [message, setMessage] = useState<Message>(null);
  const statusLabel = (status: string) => ({ pending: t('待处理', 'Pending'), accepted: t('已接受', 'Accepted'), in_progress: t('进行中', 'In progress'), completed: t('已完成', 'Completed'), done: t('已完成', 'Completed'), rejected: t('已拒绝', 'Rejected') }[status] || status || t('未知', 'Unknown'));
  const load = async () => { setLoading(true); setMessage(null); try { const [taskResp, workerResp] = await Promise.all([requestJSON<{ collaborations?: CollabTask[]; tasks?: CollabTask[] }>('/admin/collaborations'), requestJSON<{ colleagues?: Colleague[] }>('/admin/colleagues')]); const nextWorkers = workerResp.colleagues || []; setTasks(taskResp.collaborations || taskResp.tasks || []); setWorkers(nextWorkers); setForm(current => current.from_colleague_id || nextWorkers.length === 0 ? current : { ...current, from_colleague_id: nextWorkers[0].id, to_colleague_id: nextWorkers[0].id }); } catch (err) { setMessage({ kind: 'danger', text: err instanceof Error ? err.message : t('加载协作任务失败。', 'Failed to load collaboration tasks.') }); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, []);
  const summary = useMemo(() => ({ pending: tasks.filter(task => task.status === 'pending' || task.status === 'accepted').length, running: tasks.filter(task => task.status === 'in_progress').length, completed: tasks.filter(task => task.status === 'completed' || task.status === 'done').length, roleRouted: tasks.filter(task => !task.to_colleague_id && task.to_role_code).length }), [tasks]);
  const rows = useMemo<CollabRow[]>(() => tasks.map(task => ({ id: shortID(task.id), title: task.title || task.id, from: workerName(workers, task.from_colleague_id), to: task.to_role_code || workerName(workers, task.to_colleague_id), status: statusLabel(task.status), priority: String(task.priority ?? '-'), updated: task.updated_at || task.created_at || '-' })), [tasks, workers, t]);
  const createTask = async () => { setBusy('create'); setMessage(null); try { await requestJSON('/admin/collaborations', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title: form.title.trim(), description: form.description.trim(), from_colleague_id: form.from_colleague_id, to_colleague_id: form.to_colleague_id, to_role_code: form.to_role_code.trim(), priority: Number(form.priority) || 5 }) }); setForm({ ...emptyForm, from_colleague_id: form.from_colleague_id, to_colleague_id: form.to_colleague_id }); setMessage({ kind: 'ok', text: t('协作任务已创建。', 'Collaboration task created.') }); await load(); } catch (err) { setMessage({ kind: 'danger', text: err instanceof Error ? err.message : t('创建失败。', 'Create failed.') }); } finally { setBusy(''); } };
  return <div className="center-page-stack"><SectionCard title={t('协作流转', 'Collaboration Flow')} desc={t('跟踪 iWorker 到 iWorker、iWorker 到人类员工的交接。待处理项也应在 iWorker 客户端显示成醒目的干预提醒。', 'Track iWorker-to-iWorker and iWorker-to-human handoffs. Pending items should surface as visible intervention reminders in the iWorker client.')}><div className="cloud-status-grid cloud-status-grid-wide"><StatusTile label={t('待处理', 'Pending')} value={String(summary.pending)} tone={summary.pending ? 'warn' : 'ok'} /><StatusTile label={t('运行中', 'Running')} value={String(summary.running)} /><StatusTile label={t('已完成', 'Completed')} value={String(summary.completed)} tone="ok" /><StatusTile label={t('按角色路由', 'Role routed')} value={String(summary.roleRouted)} /></div><div className="cloud-actions"><button className="ghost" type="button" onClick={() => { void load(); }} disabled={loading}>{loading ? t('刷新中...', 'Refreshing...') : t('刷新任务', 'Refresh tasks')}</button><span className="cloud-inline-note">{t('来源：Center API', 'Source: Center API')}</span></div>{summary.pending > 0 ? <p className="cloud-message warn">{t(summary.pending + ' 个任务正在等待处理。请确认目标 iWorker 是否显示了人工干预卡片。', summary.pending + ' task(s) are waiting for action. Check whether the target iWorker has a visible intervention card.')}</p> : null}{message ? <p className={'cloud-message ' + message.kind}>{message.text}</p> : null}</SectionCard><SectionCard title={t('创建交接', 'Create Handoff')} desc={t('当人类员工或操作员需要把工作导入数字员工队伍时，可以创建一个手工协作项。', 'Create a manual collaboration item when a human employee or operator needs to direct work into the digital workforce.')}><div className="cloud-form-grid"><label className="cloud-field"><span>{t('标题', 'Title')}</span><input value={form.title} onChange={e => setForm({ ...form, title: e.target.value })} /></label><label className="cloud-field"><span>{t('优先级', 'Priority')}</span><input type="number" min="1" max="10" value={form.priority} onChange={e => setForm({ ...form, priority: e.target.value })} /></label><label className="cloud-field"><span>{t('发起 iWorker', 'From iWorker')}</span><select value={form.from_colleague_id} onChange={e => setForm({ ...form, from_colleague_id: e.target.value })}>{workers.length === 0 ? <option value="">{t('无 iWorker', 'No iWorkers')}</option> : workers.map(worker => <option key={worker.id} value={worker.id}>{worker.name}</option>)}</select></label><label className="cloud-field"><span>{t('目标 iWorker', 'To iWorker')}</span><select value={form.to_colleague_id} onChange={e => setForm({ ...form, to_colleague_id: e.target.value })}>{workers.length === 0 ? <option value="">{t('无 iWorker', 'No iWorkers')}</option> : workers.map(worker => <option key={worker.id} value={worker.id}>{worker.name}</option>)}</select></label><label className="cloud-field"><span>{t('角色路由覆盖', 'Role route override')}</span><input value={form.to_role_code} onChange={e => setForm({ ...form, to_role_code: e.target.value })} placeholder={t('可选角色代码', 'Optional role code')} /></label><label className="cloud-field cloud-field-wide"><span>{t('描述', 'Description')}</span><textarea rows={4} value={form.description} onChange={e => setForm({ ...form, description: e.target.value })} /></label></div><div className="cloud-actions"><button className="cloud-primary" type="button" disabled={busy === 'create' || !form.title.trim() || !form.from_colleague_id || (!form.to_colleague_id && !form.to_role_code.trim())} onClick={() => { void createTask(); }}>{busy === 'create' ? t('创建中...', 'Creating...') : t('创建交接', 'Create handoff')}</button></div></SectionCard><SectionCard title={t('交接记录', 'Handoff Records')} desc={t('共 ' + tasks.length + ' 个协作任务。', 'Total ' + tasks.length + ' collaboration tasks.')}><DataTable columns={[{ key: 'id', label: t('任务 ID', 'Task ID') }, { key: 'title', label: t('标题', 'Title') }, { key: 'from', label: t('发起方', 'From') }, { key: 'to', label: t('目标', 'To') }, { key: 'status', label: t('状态', 'Status') }, { key: 'priority', label: t('优先级', 'Priority') }, { key: 'updated', label: t('更新时间', 'Updated') }]} rows={rows} />{tasks.length === 0 ? <p className="cloud-inline-note">{t('暂无协作记录。流程或手工交接创建的任务会显示在这里。', 'No collaboration records yet. Tasks created by workflows or manual handoffs will appear here.')}</p> : null}</SectionCard></div>;
}
function StatusTile({ label, value, tone }: { label: string; value: string; tone?: 'ok' | 'warn' }) { return <div className={'cloud-status-tile ' + (tone || '')}><span>{label}</span><strong>{value}</strong></div>; }
