import { useEffect, useMemo, useState } from 'react';
import { SectionCard } from '../components/cards/SectionCard';
import { DataTable } from '../components/table/DataTable';
import { useI18n } from '../i18n';

type Provider = { id: string; name: string; protocol: string; base_url: string; model: string; priority: number; features: string[]; description: string; enabled: boolean; cost_tier: string };
type CenterSettings = { providers: Provider[]; work_type_keywords?: Record<string, string[]>; work_type_tier?: Record<string, string>; role_provider_boost?: Record<string, string[]> };
type DBEndpoint = { id: string; name: string; protocol: string; base_url?: string; api_key?: string; model: string; cost_tier: string; priority: number; features?: string; status: string };
type DBPolicy = { id: string; name: string; description?: string; work_type: string; role_code: string; endpoint_id: string; fallback_mode: string; priority: number; status: string };
type Message = { kind: 'ok' | 'warn' | 'danger'; text: string };
const hasWails = () => typeof window !== 'undefined' && typeof (window as Window & { go?: unknown }).go !== 'undefined';
const emptyEndpoint = () => ({ name: '', protocol: 'openai', base_url: '', api_key: '', model: '', cost_tier: 'medium', priority: 10, features: '[]' });
const emptyPolicy = () => ({ name: '', description: '', work_type: '*', role_code: '*', endpoint_id: '', fallback_mode: 'next_priority', priority: 10 });
async function requestJSON<T>(url: string, init?: RequestInit): Promise<T> { const resp = await fetch(url, init); const text = await resp.text(); const data = text ? JSON.parse(text) : null; if (!resp.ok) throw new Error(data?.error?.message || data?.message || 'Request failed: ' + resp.status); return data as T; }
async function fetchJSON<T>(url: string): Promise<T | null> { try { return await requestJSON<T>(url); } catch { return null; } }

export function ModelRoutingPage() {
  const { t } = useI18n();
  const [settingsRows, setSettingsRows] = useState<Array<Record<string, string>>>([]);
  const [settingsRoutingRows, setSettingsRoutingRows] = useState<Array<Record<string, string>>>([]);
  const [dbEndpoints, setDbEndpoints] = useState<DBEndpoint[]>([]);
  const [dbPolicies, setDbPolicies] = useState<DBPolicy[]>([]);
  const [endpointForm, setEndpointForm] = useState(emptyEndpoint());
  const [policyForm, setPolicyForm] = useState(emptyPolicy());
  const [loading, setLoading] = useState(false);
  const [busy, setBusy] = useState('');
  const [message, setMessage] = useState<Message | null>(null);
  const tierLabel = (tier: string) => ({ low: t('低成本', 'Low cost'), medium: t('均衡', 'Balanced'), high: t('高能力', 'High capability') }[tier] || tier || t('未设置', 'Unset'));
  const statusLabel = (status: string) => ({ active: t('启用', 'Active'), disabled: t('停用', 'Disabled') }[status] || status || t('未知', 'Unknown'));
  const load = async () => { setLoading(true); setMessage(null); try { const [endpointResp, policyResp] = await Promise.all([fetchJSON<{ endpoints: DBEndpoint[] }>('/admin/model-endpoints'), fetchJSON<{ policies: DBPolicy[] }>('/admin/model-routing-policies')]); setDbEndpoints(endpointResp?.endpoints || []); setDbPolicies(policyResp?.policies || []); if (!policyForm.endpoint_id && endpointResp?.endpoints?.[0]) setPolicyForm(current => ({ ...current, endpoint_id: endpointResp.endpoints[0].id })); } catch (err) { setMessage({ kind: 'danger', text: err instanceof Error ? err.message : t('加载模型配置失败。', 'Failed to load model configuration.') }); } finally { setLoading(false); } };
  useEffect(() => { void load(); if (!hasWails()) return; (window as any).go.main.App.LoadCenterSettings().then((settings: CenterSettings) => { const providers = settings?.providers || []; setSettingsRows(providers.map(p => ({ name: p.name || p.id, model: p.model, protocol: p.protocol || 'openai', tier: tierLabel(p.cost_tier), priority: String(p.priority), status: p.enabled ? t('启用', 'Active') : t('停用', 'Disabled') }))); if (settings?.work_type_tier && Object.keys(settings.work_type_tier).length > 0) setSettingsRoutingRows(Object.entries(settings.work_type_tier).map(([workType, tier]) => { const tierProviders = providers.filter(p => p.enabled && p.cost_tier === tier); return { scene: workType, primary: tierProviders[0]?.model || '-', backup: tierProviders[1]?.model || '-', route: tierLabel(tier) + t(' 路由', ' route') }; })); }).catch(() => {}); }, []);
  const summary = useMemo(() => ({ activeEndpoints: dbEndpoints.filter(e => e.status === 'active').length, activePolicies: dbPolicies.filter(p => p.status === 'active').length, highTier: dbEndpoints.filter(e => e.cost_tier === 'high').length }), [dbEndpoints, dbPolicies]);
  const createEndpoint = async () => { if (!endpointForm.name.trim() || !endpointForm.model.trim()) { setMessage({ kind: 'warn', text: t('请填写端点名称和模型名称。', 'Enter endpoint name and model name.') }); return; } setBusy('endpoint'); setMessage(null); try { await requestJSON<DBEndpoint>('/admin/model-endpoints', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...endpointForm, priority: Number(endpointForm.priority) || 0 }) }); setEndpointForm(emptyEndpoint()); setMessage({ kind: 'ok', text: t('模型端点已创建。', 'Model endpoint created.') }); await load(); } catch (err) { setMessage({ kind: 'danger', text: err instanceof Error ? err.message : t('创建模型端点失败。', 'Failed to create model endpoint.') }); } finally { setBusy(''); } };
  const createPolicy = async () => { if (!policyForm.name.trim() || !policyForm.endpoint_id.trim()) { setMessage({ kind: 'warn', text: t('请填写策略名称并选择模型端点。', 'Enter policy name and select a model endpoint.') }); return; } setBusy('policy'); setMessage(null); try { await requestJSON<DBPolicy>('/admin/model-routing-policies', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...policyForm, priority: Number(policyForm.priority) || 0 }) }); setPolicyForm({ ...emptyPolicy(), endpoint_id: dbEndpoints[0]?.id || '' }); setMessage({ kind: 'ok', text: t('模型路由策略已创建。', 'Model routing policy created.') }); await load(); } catch (err) { setMessage({ kind: 'danger', text: err instanceof Error ? err.message : t('创建路由策略失败。', 'Failed to create routing policy.') }); } finally { setBusy(''); } };
  const endpointRows = dbEndpoints.map(e => ({ name: e.name, model: e.model, protocol: e.protocol, tier: tierLabel(e.cost_tier), priority: String(e.priority), status: statusLabel(e.status) }));
  const policyRows = dbPolicies.map(p => ({ name: p.name, work_type: p.work_type === '*' ? t('全部', 'All') : p.work_type, role: p.role_code === '*' ? t('全部', 'All') : p.role_code, endpoint: dbEndpoints.find(e => e.id === p.endpoint_id)?.name || p.endpoint_id || '-', fallback: p.fallback_mode === 'next_priority' ? t('按优先级回退', 'Fallback by priority') : p.fallback_mode, priority: String(p.priority), status: statusLabel(p.status) }));
  return <div className="center-page-stack"><SectionCard title={t('模型调度总览', 'Model Routing Overview')} desc={t('Center 负责本地模型端点和路由策略，iWorker 执行任务时按工作类型、角色和优先级选择模型。Cloud 离线不影响已保存的本地路由。', 'Center owns local model endpoints and routing policies. iWorkers choose models by work type, role, and priority. Saved local routing continues when Cloud is offline.')}><div className="cloud-status-grid"><StatusTile label={t('启用端点', 'Active endpoints')} value={String(summary.activeEndpoints)} tone="ok" /><StatusTile label={t('启用策略', 'Active policies')} value={String(summary.activePolicies)} tone="ok" /><StatusTile label={t('高能力端点', 'High-tier endpoints')} value={String(summary.highTier)} tone={summary.highTier ? 'warn' : 'ok'} /></div><div className="cloud-actions"><button className="ghost" type="button" onClick={() => { void load(); }} disabled={loading}>{loading ? t('刷新中...', 'Refreshing...') : t('刷新模型配置', 'Refresh model config')}</button></div>{message && <p className={'cloud-message ' + message.kind}>{message.text}</p>}</SectionCard><SectionCard title={t('新增模型端点', 'Add Model Endpoint')} desc={t('端点保存到 Center 本地数据库。API Key 只在创建/更新时提交，列表中由后端脱敏。', 'Endpoints are stored in the local Center database. API keys are submitted only when creating/updating and are masked by the backend in lists.')}><div className="cloud-form-grid"><label className="cloud-field"><span>{t('端点名称', 'Endpoint name')}</span><input value={endpointForm.name} onChange={e => setEndpointForm({ ...endpointForm, name: e.target.value })} placeholder={t('例如：公司 OpenAI 网关', 'Example: Company OpenAI gateway')} /></label><label className="cloud-field"><span>{t('协议', 'Protocol')}</span><select value={endpointForm.protocol} onChange={e => setEndpointForm({ ...endpointForm, protocol: e.target.value })}><option value="openai">OpenAI Compatible</option><option value="anthropic">Anthropic</option></select></label><label className="cloud-field"><span>Base URL</span><input value={endpointForm.base_url} onChange={e => setEndpointForm({ ...endpointForm, base_url: e.target.value })} placeholder="https://api.example.com/v1" /></label><label className="cloud-field"><span>API Key</span><input type="password" value={endpointForm.api_key} onChange={e => setEndpointForm({ ...endpointForm, api_key: e.target.value })} placeholder={t('可留空用于本地代理', 'Optional when using a local proxy')} /></label><label className="cloud-field"><span>{t('模型', 'Model')}</span><input value={endpointForm.model} onChange={e => setEndpointForm({ ...endpointForm, model: e.target.value })} placeholder="gpt-4.1-mini / qwen-plus / ..." /></label><label className="cloud-field"><span>{t('成本层级', 'Cost tier')}</span><select value={endpointForm.cost_tier} onChange={e => setEndpointForm({ ...endpointForm, cost_tier: e.target.value })}><option value="low">{t('低成本', 'Low cost')}</option><option value="medium">{t('均衡', 'Balanced')}</option><option value="high">{t('高能力', 'High capability')}</option></select></label><label className="cloud-field"><span>{t('优先级', 'Priority')}</span><input type="number" value={endpointForm.priority} onChange={e => setEndpointForm({ ...endpointForm, priority: Number(e.target.value) || 0 })} /></label><label className="cloud-field"><span>{t('特性 JSON', 'Features JSON')}</span><input value={endpointForm.features} onChange={e => setEndpointForm({ ...endpointForm, features: e.target.value })} placeholder='["tool", "vision"]' /></label></div><div className="cloud-actions"><button className="cloud-primary" onClick={createEndpoint} disabled={busy === 'endpoint'}>{busy === 'endpoint' ? t('创建中...', 'Creating...') : t('创建端点', 'Create endpoint')}</button></div></SectionCard><SectionCard title={t('模型端点', 'Model Endpoints')} desc={t('共 ' + dbEndpoints.length + ' 个数据库管理端点。', 'Total ' + dbEndpoints.length + ' database-managed endpoints.')}><DataTable columns={[{ key: 'name', label: t('名称', 'Name') }, { key: 'model', label: t('模型', 'Model') }, { key: 'protocol', label: t('协议', 'Protocol') }, { key: 'tier', label: t('成本层级', 'Cost tier') }, { key: 'priority', label: t('优先级', 'Priority') }, { key: 'status', label: t('状态', 'Status') }]} rows={endpointRows} />{dbEndpoints.length === 0 && <p className="cloud-inline-note">{t('暂无数据库端点。请先创建至少一个模型端点，iWorker 才能使用 Center 的动态路由。', 'No database endpoints yet. Create at least one model endpoint before iWorkers can use Center dynamic routing.')}</p>}</SectionCard><SectionCard title={t('新增路由策略', 'Add Routing Policy')} desc={t('策略按优先级匹配工作类型和角色。未命中时使用启用端点的优先级回退。', 'Policies match work type and role by priority. If no policy matches, enabled endpoints are used by priority fallback.')}><div className="cloud-form-grid"><label className="cloud-field"><span>{t('策略名称', 'Policy name')}</span><input value={policyForm.name} onChange={e => setPolicyForm({ ...policyForm, name: e.target.value })} placeholder={t('例如：数据任务优先低成本模型', 'Example: data tasks prefer low-cost model')} /></label><label className="cloud-field"><span>{t('目标端点', 'Target endpoint')}</span><select value={policyForm.endpoint_id} onChange={e => setPolicyForm({ ...policyForm, endpoint_id: e.target.value })}>{dbEndpoints.length === 0 ? <option value="">{t('暂无端点', 'No endpoint')}</option> : dbEndpoints.map(e => <option key={e.id} value={e.id}>{e.name} / {e.model}</option>)}</select></label><label className="cloud-field"><span>{t('工作类型', 'Work type')}</span><input value={policyForm.work_type} onChange={e => setPolicyForm({ ...policyForm, work_type: e.target.value })} placeholder="* / data_cleaning / writing" /></label><label className="cloud-field"><span>{t('角色代码', 'Role code')}</span><input value={policyForm.role_code} onChange={e => setPolicyForm({ ...policyForm, role_code: e.target.value })} placeholder="* / data / office" /></label><label className="cloud-field"><span>{t('回退模式', 'Fallback mode')}</span><select value={policyForm.fallback_mode} onChange={e => setPolicyForm({ ...policyForm, fallback_mode: e.target.value })}><option value="next_priority">{t('按优先级回退', 'Fallback by priority')}</option><option value="any_tier">{t('任意可用端点', 'Any available endpoint')}</option></select></label><label className="cloud-field"><span>{t('优先级', 'Priority')}</span><input type="number" value={policyForm.priority} onChange={e => setPolicyForm({ ...policyForm, priority: Number(e.target.value) || 0 })} /></label><label className="cloud-field cloud-field-wide"><span>{t('说明', 'Description')}</span><input value={policyForm.description} onChange={e => setPolicyForm({ ...policyForm, description: e.target.value })} /></label></div><div className="cloud-actions"><button className="cloud-primary" onClick={createPolicy} disabled={busy === 'policy' || dbEndpoints.length === 0}>{busy === 'policy' ? t('创建中...', 'Creating...') : t('创建路由策略', 'Create routing policy')}</button></div></SectionCard><SectionCard title={t('路由策略', 'Routing Policies')} desc={t('共 ' + dbPolicies.length + ' 条数据库管理策略。', 'Total ' + dbPolicies.length + ' database-managed policies.')}><DataTable columns={[{ key: 'name', label: t('策略名称', 'Policy') }, { key: 'work_type', label: t('工作类型', 'Work type') }, { key: 'role', label: t('角色', 'Role') }, { key: 'endpoint', label: t('目标端点', 'Endpoint') }, { key: 'fallback', label: t('回退模式', 'Fallback') }, { key: 'priority', label: t('优先级', 'Priority') }, { key: 'status', label: t('状态', 'Status') }]} rows={policyRows} />{dbPolicies.length === 0 && <p className="cloud-inline-note">{t('暂无路由策略。没有策略时，Center 会按端点优先级选择默认模型。', 'No routing policies yet. Without policies, Center chooses models by endpoint priority.')}</p>}</SectionCard>{settingsRows.length > 0 && <SectionCard title={t('配置文件提供商', 'Settings File Providers')} desc={t('来自 settings.json 的兼容配置，保留用于迁移和回退。', 'Compatible settings from settings.json, kept for migration and rollback.')}><DataTable columns={[{ key: 'name', label: t('名称', 'Name') }, { key: 'model', label: t('模型', 'Model') }, { key: 'protocol', label: t('协议', 'Protocol') }, { key: 'tier', label: t('成本层级', 'Cost tier') }, { key: 'priority', label: t('优先级', 'Priority') }, { key: 'status', label: t('状态', 'Status') }]} rows={settingsRows} /></SectionCard>}{settingsRoutingRows.length > 0 && <SectionCard title={t('配置文件路由策略', 'Settings File Routing')} desc={t('来自 settings.json 的路由规则。', 'Routing rules from settings.json.')}><DataTable columns={[{ key: 'scene', label: t('场景', 'Scene') }, { key: 'primary', label: t('默认模型', 'Primary model') }, { key: 'backup', label: t('备用模型', 'Backup model') }, { key: 'route', label: t('路由策略', 'Routing policy') }]} rows={settingsRoutingRows} /></SectionCard>}</div>;
}
function StatusTile({ label, value, tone }: { label: string; value: string; tone?: 'ok' | 'warn' }) { return <div className={'cloud-status-tile ' + (tone || '')}><span>{label}</span><strong>{value}</strong></div>; }
