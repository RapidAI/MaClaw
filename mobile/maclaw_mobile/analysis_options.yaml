include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Existing call sites still omit trailing commas; keep the lint for new
    # edits without failing `flutter analyze` on the current tree.
    require_trailing_commas: ignore

linter:
  rules:
    prefer_single_quotes: true
    require_trailing_commas: true

