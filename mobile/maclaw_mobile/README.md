# MaClaw Mobile

An Android/iOS Flutter app for the MaClaw mobile AI assistant, shaped as a
MaClaw GUI-like assistant workspace:

- a MaClaw GUI-like multi-tab AI assistant that opens as the signed-in first
  workspace, with typed or voice input as primary interaction modes;
- assistant online answers with citations, shareable results, screenshot/photo
  questions, and document handoff;
- urgent document drafting, editing, and export;
- backend SSH session management with AI-assisted log explanation;
- digital employee access for remote server or desktop capabilities;
- Hub account, quota, service status, cache, and credential management.

The app uses only the three preset official HubCenter endpoints
(`https://hubs.mypapers.top`, `https://hubs.maclaw.top`,
`https://hubs2.maclaw.top`) to discover the user's Hub and tenant. It does not
expose custom Hub endpoint configuration. Third-party LLM access is available
only as an optional account/settings action through a QR authorization generated
by MaClaw desktop GUI. The first screen for an unregistered or signed-out user
is phone registration/login through the discovered Hub SMS verification flow.

Mobile keeps lightweight local cache for assistant history, server metadata,
common commands, digital employee prompts, and the most recent document draft.
Sensitive tokens and SSH credentials stay in secure storage.

## Runtime boundary

MaClaw Mobile does not embed or directly call the Go `corelib` package. The
Flutter app keeps only mobile-facing capabilities such as UI, cache, sharing,
document handoff, secure credential storage, and remote session controls. Core MaClaw
capabilities run behind the official Hub or on authorized remote desktop/server
digital employees, and the phone reaches them through the discovered Hub APIs
and realtime channel.

## Commands

```bash
flutter pub get
flutter test --concurrency=1
flutter build apk --debug
flutter run
```

Generate native wrappers if needed:

```bash
flutter create --platforms android,ios .
python3 tool/configure_platforms.py
```

## Native sharing

Android share/deep-link intent filters are generated into
`android/app/src/main/AndroidManifest.xml`.

iOS needs a real Share Extension target in the official Apple developer team:

- bundle id: `top.mypapers.maclaw.mobile.ShareExtension`;
- app group: `group.top.mypapers.maclaw.mobile`;
- target files generated by `tool/configure_platforms.py` under
  `ios/ShareExtension/`;
- Runner URL schemes: `maclaw` and `ShareMedia-$(PRODUCT_BUNDLE_IDENTIFIER)`;
- Runner and Share Extension entitlements must both include
  `$(CUSTOM_GROUP_ID)`.

The script generates the extension plist, entitlements, and
`ShareViewController.swift` that inherits `RSIShareViewController` from
`receive_sharing_intent`. Xcode still owns the signed target because Team ID and
provisioning profiles are official release settings.

The same script also maintains iOS usage descriptions for camera, microphone,
speech recognition, photo library, and local network access. Local network
permission exists only when signed-build QA exercises backend SSH session
management against a local or private-network server; the app's Hub/API traffic
uses the Hub discovered through official HubCenter.

## Hub persistence

Official Hub deployments should set `MACLAW_MOBILE_STATE_PATH` to a writable
JSON file path so mobile document drafts, upload/export jobs, and digital
employee tasks survive process restarts.

## Release

Use `docs/release_checklist.md` for HubCenter discovery, permission, native
sharing, document, SSH, digital employee, and CI checks before internal testing
or store submission. Use `docs/release_evidence.md` to separate automated
evidence from manual signed-build and real-device gates. Use
`docs/release_audit.md` for the current requirement-by-requirement release
status. Before planning signed-build QA or final approval, run
`python3 tool/release_status_report.py --scope android-ios --team-id <APPLE_TEAM_ID> --export-method development`;
`NOT READY` means the listed signing inputs, QA records, or final evidence must
be resolved first. Use
`docs/qa_device_checklist.md` for signed-build and real-device QA. Use
`docs/qa_build_record_template.md` to record one signed QA build's evidence.
Store completed records under `docs/qa-builds/`; see
`docs/qa-builds/README.md` for naming and redaction rules.

## User guide

Use `docs/user_guide.md` for the first-version mobile flows: AI assistant,
emergency documents, backend SSH session management, digital employees, and
account privacy settings.

See `docs/backend_ssh_session_design.md` for the corrected GUI-like backend SSH
session model, the mobile Hub API shape, and the remaining Hub/agent work needed
for realtime managed-session output.
