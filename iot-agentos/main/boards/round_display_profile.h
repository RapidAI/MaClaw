#pragma once

/*
 * This header intentionally no longer carries panel transport facts.  Those
 * values belong to the selected display adapter and are exposed only through
 * round_display_service.  Keep the file as a harmless compatibility stub for
 * out-of-tree profile code while new profiles migrate to that service seam.
 */
