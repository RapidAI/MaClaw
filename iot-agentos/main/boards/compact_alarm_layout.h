/* Physical alarm-scene geometry for compact display profiles. */
#pragma once

typedef struct {
    int accent_y;
    int title_y;
    int title_scale;
    int time_y;
    int label_y;
    int label_scale;
    int attempt_y;
    int instruction_y;
    int instruction_scale;
} compact_alarm_layout_t;
