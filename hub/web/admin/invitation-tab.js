/*
 * Invitation codes admin module.
 * ASCII only.
 */
// invitation.js - Invitation code management
async function loadInvitationCodeStatus() { try { const data = await api('/api/admin/invitation-codes/status'); invitationCodeRequired = !!data.invitation_code_required; renderInvitationCodeToggle(); } catch (err) { const msg = inv('statusLoadFailed', { error: err.message }); setOutput(msg); } }
async function toggleInvitationCodeRequired() { try { const newVal = !invitationCodeRequired; const data = await api('/api/admin/invitation-codes/toggle', { method: 'POST', body: JSON.stringify({ required: newVal }) }); invitationCodeRequired = !!data.invitation_code_required; renderInvitationCodeToggle(); const msg = inv('toggleSuccess'); setOutput(msg); showToast(msg, 'success'); } catch (err) { const msg = inv('toggleFailed', { error: err.message }); setOutput(msg); showToast(msg, 'error'); } }
function renderInvitationCodeToggle() { const badge = document.getElementById('invCodeToggleBadge'); const btn = document.getElementById('invCodeToggleBtn'); if (badge) { badge.textContent = invitationCodeRequired ? inv('toggleEnabled') : inv('toggleDisabled'); badge.className = 'badge ' + (invitationCodeRequired ? 'ok' : 'warn'); } if (btn) { btn.textContent = invitationCodeRequired ? inv('toggleDisabled') : inv('toggleEnabled'); btn.className = invitationCodeRequired ? 'btn-danger' : 'btn-primary'; } }
async function generateInvitationCodes() { try { const countInput = document.getElementById('invCodeCount'); const count = parseInt(countInput.value, 10) || 5; let validity_days = 0; const permanent = document.getElementById('invCodePermanent'); if (!permanent || !permanent.checked) { const valInput = document.getElementById('invCodeValidityValue'); const unitSelect = document.getElementById('invCodeValidityUnit'); const valNum = parseInt(valInput?.value, 10) || 0; const unitMul = parseInt(unitSelect?.value, 10) || 1; validity_days = valNum > 0 ? valNum * unitMul : 0; } const vip = !!document.getElementById('invCodeVIP')?.checked; const data = await api('/api/admin/invitation-codes/generate', { method: 'POST', body: JSON.stringify({ count, validity_days, vip }) }); const msg = inv('generateSuccess', { count: String((data.codes || []).length) }); setOutput(msg); showToast(msg, 'success'); await loadInvitationCodes(); } catch (err) { const msg = inv('generateFailed', { error: err.message }); setOutput(msg); showToast(msg, 'error'); } }
function toggleValidityInputs() { const cb = document.getElementById('invCodePermanent'); const row = document.getElementById('invCodeValidityRow'); if (row) row.style.display = (cb && cb.checked) ? 'none' : 'block'; }
async function loadInvitationCodes() { try { let path = '/api/admin/invitation-codes'; const params = []; if (invitationStatusFilter) params.push('status=' + encodeURIComponent(invitationStatusFilter)); if (invitationSearchTerm) params.push('search=' + encodeURIComponent(invitationSearchTerm)); params.push('page=' + invitationPage); params.push('page_size=' + invitationPageSize); if (params.length) path += '?' + params.join('&'); const data = await api(path); invitationCodesCache = data.codes || []; invitationCodesTotal = data.total || 0; renderInvitationCodeList(); } catch (err) { const msg = inv('loadFailed', { error: err.message }); setOutput(msg); showToast(msg, 'error'); } }
let invitationSearchTimer = null;
function onInvitationSearch() { clearTimeout(invitationSearchTimer); invitationSearchTimer = setTimeout(() => { invitationSearchTerm = (document.getElementById('invCodeSearch')?.value || '').trim(); invitationPage = 1; loadInvitationCodes(); }, 300); }
function onInvitationFilterChange() { invitationStatusFilter = document.getElementById('invCodeFilter')?.value || ''; invitationPage = 1; loadInvitationCodes(); }
function getValidityDisplay(c) { const vd = c.validity_days || 0; if (vd === 0) return { text: inv('validityPermanentLabel'), style: '' }; if (c.status === 'used' && c.bound_at) { const boundDate = new Date(c.bound_at); const now = new Date(); const elapsed = Math.floor((now - boundDate) / (1000 * 60 * 60 * 24)); const remaining = vd - elapsed; if (remaining <= 0) return { text: inv('validityExpired'), style: 'color:#e53935;font-weight:600' }; return { text: inv('validityRemaining', { days: String(remaining) }), style: 'color:#43a047' }; } return { text: inv('validityDaysLabel', { days: String(vd) }), style: '' }; }
function renderInvitationCodeList() { const root = document.getElementById('invitationCodeList'); const pager = document.getElementById('invitationCodesPager'); const pagerMeta = document.getElementById('invitationCodesPagerMeta'); const prevBtn = document.getElementById('invitationCodesPrevButton'); const nextBtn = document.getElementById('invitationCodesNextButton'); if (!root) return; if (!invitationCodesCache.length) { root.innerHTML = '<div class="hint">' + inv('emptyList') + '</div>'; if (pager) pager.classList.add('hidden'); return; } const header = '<div class="row header" style="grid-template-columns:1.28fr .72fr .84fr 1fr .84fr .68fr"><div>' + inv('codeLabel') + '</div><div>Status</div><div>' + inv('validityInfo') + '</div><div>' + inv('usedBy') + '</div><div>' + inv('createdAt') + '</div><div></div></div>'; const rows = invitationCodesCache.map(c => { const isUsed = c.status === 'used'; const validity = getValidityDisplay(c); const isExpired = validity.text === inv('validityExpired'); const statusClass = isExpired ? 'danger' : (isUsed ? 'warn' : 'ok'); const statusText = isExpired ? inv('validityExpired') : (isUsed ? inv('statusUsed') : inv('statusUnused')); const exportBadge = (!isUsed && c.exported) ? '<span class="badge info" style="margin-left:4px">' + inv('exportedLabel') + '</span>' : ''; const vipBadge = c.vip ? '<span class="badge warn" style="margin-right:6px">VIP</span>' : ''; const action = isUsed ? '<button class="btn-danger" type="button" style="height:28px;font-size:11px;padding:0 8px" onclick="unbindInvitationCode(\'' + c.id + '\',\'' + escapeHtml(c.used_by_email || '').replace(/'/g, "\\'") + '\')">' + inv('unbind') + '</button>' : '<span class="item-meta">-</span>'; const rowStyle = isExpired ? 'border-color:rgba(214,93,87,.2);background:#fff9f9' : (c.vip ? 'background:#fffdf5;border-color:rgba(245,166,35,.16)' : ''); return '<div class="row" style="grid-template-columns:1.28fr .72fr .84fr 1fr .84fr .68fr;' + rowStyle + '"><div style="min-width:0"><div class="mono" style="font-size:11px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">' + vipBadge + escapeHtml(c.code || '-') + '</div></div><div><span class="badge ' + statusClass + '">' + statusText + '</span>' + exportBadge + '</div><div class="item-meta" style="' + (validity.style || '') + '">' + validity.text + '</div><div class="item-meta mono" style="white-space:nowrap;overflow:hidden;text-overflow:ellipsis">' + escapeHtml(c.used_by_email || '-') + '</div><div class="item-meta">' + escapeHtml(c.created_at || '-') + '</div><div style="display:flex;justify-content:flex-end">' + action + '</div></div>'; }).join(''); root.innerHTML = '<div class="table" style="gap:6px">' + header + rows + '</div>'; if (pager) { const totalPages = Math.max(1, Math.ceil(invitationCodesTotal / invitationPageSize)); const start = (invitationPage - 1) * invitationPageSize + 1; const end = start + invitationCodesCache.length - 1; if (pagerMeta) pagerMeta.textContent = invitationCodesTotal > invitationPageSize ? inv('pageSummary', { start: String(start), end: String(end), total: String(invitationCodesTotal) }) : inv('pageSingle', { total: String(invitationCodesTotal) }); if (prevBtn) prevBtn.disabled = invitationPage <= 1; if (nextBtn) nextBtn.disabled = invitationPage >= totalPages; pager.classList.toggle('hidden', invitationCodesTotal <= invitationPageSize); } }
async function unbindInvitationCode(id, email) { if (!confirm(inv('unbindConfirm', { email: email }))) return; try { await api('/api/admin/invitation-codes/unbind', { method: 'POST', body: JSON.stringify({ id: id }) }); const msg = inv('unbindSuccess'); setOutput(msg); showToast(msg, 'success'); await loadInvitationCodes(); } catch (err) { const msg = inv('unbindFailed', { error: err.message }); setOutput(msg); showToast(msg, 'error'); } }
function changeInvitationPage(step) { const totalPages = Math.max(1, Math.ceil(invitationCodesTotal / invitationPageSize)); invitationPage = Math.min(totalPages, Math.max(1, invitationPage + step)); loadInvitationCodes(); }
function exportInvitationCodes() { const exportedFilter = document.getElementById('invCodeExportFilter')?.value || 'unexported'; const vipOnly = document.getElementById('invCodeExportVIP')?.checked ? 'true' : ''; let url = '/api/admin/invitation-codes/export?exported=' + encodeURIComponent(exportedFilter); if (vipOnly) url += '&vip=true'; const headers = {}; if (token()) headers['Authorization'] = 'Bearer ' + token(); fetch(url, { headers }).then(r => { const count = r.headers.get('X-Export-Count') || '?'; return r.blob().then(blob => ({ blob, count })); }).then(({ blob, count }) => { if (blob.size <= 1) { const msg = inv('exportEmpty'); setOutput(msg); showToast(msg, 'info'); return; } const blobUrl = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = blobUrl; a.download = 'invitation-codes.txt'; a.click(); URL.revokeObjectURL(blobUrl); const msg = inv('exportSuccess', { count }); setOutput(msg); showToast(msg, 'success'); loadInvitationCodes(); }).catch(err => { const msg = inv('loadFailed', { error: err.message || 'export failed' }); setOutput(msg); showToast(msg, 'error'); }); }
