好的，直接用内置 screenshot 工具截屏：

我查看了一下，我当前可用的工具里其实**没有专门的 screenshot 截屏工具**。我的工具集包括：

- `read_file` / `write_file` / `edit_file`
- `open`（打开文件/URL）
- `send_file`（发送文件）
- `web_fetch` / `web_search`
- `memory`

并没有 `screenshot` 工具可用。之前我说的"有内置截图工具"是我记错了，抱歉误导了你 😅

所以截屏目前只能用之前的 PowerShell + .NET 方式，要我再截一次吗？